# MAP-DATA-A-R2-F3-B · Region Vertex-to-Vertex Snap 真机验收

状态：`READY FOR USER ACCEPTANCE`。自动门禁通过不等于真机 CLOSED。

基线：`b987abe`；执行人：用户；设备/版本：________；日期：________。

## 真机验收 IPO

### F3B-M01 邻近顶点吸附

路径：区域编辑 → 区域面 → 选择一个 Region 顶点。

输入：另一区域存在可见顶点；拖动源顶点接近目标顶点。

过程：将源顶点拖入目标顶点约 8 px 的屏幕范围。

输出：预览点自动对齐目标顶点；两个 Region 仍为独立数据。

结果：________

### F3B-M02 吸附保持稳定

路径：同 F3B-M01，已进入吸附状态。

输入：在目标点附近小范围移动鼠标。

过程：观察连续 PointerMove 期间的预览点。

输出：不因轻微抖动反复切换目标或脱离吸附。

结果：________

### F3B-M03 迟滞释放

路径：同 F3B-M02。

输入：已吸附的顶点。

过程：将鼠标拉离目标点，分别观察 8 px 内、8～12 px、超过 12 px 的表现。

输出：12 px 内保持吸附；超过约 12 px 后解除并恢复自由拖动。

结果：________

### F3B-M04 当前 Region 自身排除

路径：区域编辑 → 选择一个有多个顶点的 Region → 拖动其中一个顶点。

输入：源顶点接近当前 Region 的其他顶点。

过程：在当前 Region 内移动源顶点。

输出：不吸附到当前 Region 自身的其他顶点。

结果：________

### F3B-M05 近距离候选稳定决胜

路径：区域编辑 → 选择源 Region 顶点。

输入：多个候选顶点处于相近距离。

过程：重复从相同路径拖入，并在目标附近轻微抖动。

输出：目标选择稳定，不随机跳换候选点。

结果：________

### F3B-M06 不同缩放级别

路径：区域编辑 → 选择源 Region 顶点。

输入：至少两个不同地图缩放级别。

过程：分别将顶点拖近目标顶点。

输出：吸附按屏幕空间工作，进入和释放手感基本一致。

结果：________

### F3B-M07 写回与保存重载

路径：区域编辑 → 顶点吸附 → 释放提交 → 保存地图 → 关闭并重新打开。

输入：一次成功吸附的 Region 几何。

过程：检查 Dataset、Region ID、顶点位置与图层归属。

输出：吸附后的坐标真实写回；Save/Reload 后位置和身份保持不变。

结果：________

### F3B-M08 Undo/取消回归

路径：区域编辑 → 顶点吸附拖动。

输入：一次已吸附提交和一次未释放的拖动。

过程：分别执行 Ctrl+Z、Ctrl+Y、Esc。

输出：Undo/Redo 恢复同一几何；Esc 取消预览且不新增历史。

结果：________

### F3B-M09 保留非相邻区域空白

路径：区域编辑 → 选择源 Region 顶点。

输入：目标区域距离较远或本就不相邻。

过程：拖动源顶点经过空白区域，不接近候选顶点。

输出：保持自由拖动；系统不强制填补空白或制造共享边界。

结果：________

### F3B-M10 大量 Region 拖动表现

路径：加载包含大量 Region 的地图 → 区域编辑 → 顶点拖动。

输入：大量 Region 数据集。

过程：连续拖动多个顶点，观察输入响应和编辑器稳定性。

输出：无明显卡顿、无闪退；行为符合局部查询，不退化为全图扫描。

结果：________

## 总体结论

总体：________；备注：________。

验收规则：全部 M01～M10 PASS 后，F3-B 才可由 `READY FOR USER ACCEPTANCE` 改为 `CLOSED`；在此之前不得启动 F3-C、Road Snap 或 Topology Weld。
