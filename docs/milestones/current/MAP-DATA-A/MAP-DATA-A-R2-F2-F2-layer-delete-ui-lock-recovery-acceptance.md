# MAP-DATA-A-R2-F2-F2 · Layer Delete UI Lock Recovery 真机验收

基线：`d426d007`；状态：`CLOSED`。用户已裁定 C01～C07 全 PASS。

## F2-F2-M01 道路图层删除 → 取消

路径：道路图层 → 删除 → 取消。

输出：删除未发生，顶部、左侧、右侧可继续操作，Esc 可再次取消普通操作。

结果：通过

## F2-F2-M02 道路图层删除 → 确认

路径：道路图层 → 删除 → 确认。

输出：图层删除，确认 UI 消失，顶部、左侧、右侧全部恢复可用。

结果：通过

## F2-F2-M03 区域面图层删除 → 取消

路径：区域面图层 → 删除 → 取消。

输出：删除未发生，主 UI 恢复可用。

结果：通过

## F2-F2-M04 删除当前选中图层

路径：选中当前活动图层 → 删除 → 确认。

输出：活动图层转移到有效相邻区域图层；当前 Layer、Inspector、列表状态同步；主 UI 可继续操作。

结果：通过

## F2-F2-M05 删除失败/拒绝

路径：删除最后一个区域图层、系统图层或其他拒绝路径。

输出：删除被拒绝，错误反馈可见；确认状态清除；主 UI 不锁死。

结果：通过

## F2-F2-M06 删除结束后继续操作

路径：完成取消、确认或拒绝后，继续点击视口、工具栏、左栏和右栏。

输出：所有区域恢复可用；不出现幽灵遮罩；不需要 Enter/Space 解锁。

结果：通过

## 原事故路径 M10

状态：`CLOSED`；原事故路径的修复与回归已由本轮 C01～C07 真机验收完成。

路径：按原 M10 清单执行。

## 总体

总体：`CLOSED`。
