# MAP-DATA-A-R2-F2 · Geometry Vertex Editing

## 真机验收 IPO

基线：`d426d007`；执行人：用户；2026-08-12 C01～C07 全 PASS，F3 已解除冻结。

### F2-M01 区域选择与控制柄

输入：进入“区域编辑 / 区域面”，加载含已完成区域的数据集。

过程：点击区域面，观察边界与顶点控制柄。

输出：区域被选中，所有区域顶点控制柄可见；不重新进入绘制 Draft。

结果：通过（C01）

### F2-M02 道路选择与控制柄

输入：切换“道路”，加载含已完成道路的数据集。

过程：点击道路线，观察节点控制柄。

输出：道路被选中，所有道路节点控制柄可见；区域与道路仍共用同一 Region 编辑上下文。

结果：通过（C02）

### F2-M03 区域顶点拖动提交

输入：已选中区域，抓取一个顶点移动到地图合法范围内。

过程：按住鼠标移动，观察预览；释放鼠标。

输出：预览期间地图内容未产生正式历史；释放后边界更新，产生一条区域几何历史。

结果：通过（C03）

### F2-M04 道路顶点拖动提交

输入：已选中道路，抓取一个节点移动到地图合法范围内。

过程：按住鼠标移动，观察预览；释放鼠标。

输出：道路节点更新，产生一条道路几何历史；无额外重复提交。

结果：通过（C04）

### F2-M05 取消与校验拒绝

输入：分别拖动后按 Esc；将区域拖成非法多边形；将道路节点拖到相邻节点重合。

过程：观察取消、释放提交和错误反馈。

输出：Esc 恢复原几何且不入历史；非法区域/零长度道路被拒绝，原几何保持不变。

结果：通过（C05）

### F2-M06 Undo/Redo 与保存重载

输入：完成一次区域或道路顶点编辑。

过程：执行 Ctrl+Z、Ctrl+Y，保存地图，关闭后重新打开。

输出：Undo/Redo 恢复同一几何；保存重载后几何、Feature ID、Dataset/Layer 归属保持不变。

结果：通过（C06～C07）

### 总体

总体：`CLOSED`。用户已裁定 C01～C07 全 PASS；Region/Road 顶点编辑验收完成。
