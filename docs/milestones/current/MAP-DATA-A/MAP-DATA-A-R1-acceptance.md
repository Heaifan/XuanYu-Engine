# MAP-DATA-A-R1 真机验收

状态：`READY FOR USER ACCEPTANCE`。本表是本轮唯一真机验收模板；自动测试不替代人工验收。

| 序号 | 路径 | 输入 I | 过程 P | 输出 O |
|---|---|---|---|---|
| R1-M01 | 编辑模式 → 地图编辑 → 数据集 | 新建两个“区域”数据集 | 分别选择两个数据集 | 两个数据集各自显示独立 Region 图层；切换选择不串层。 |
| R1-M02 | 编辑模式 → 区域编辑 → 视口 | 在 A 绘制三点并闭合 | 切换到 B，再绘制三点并闭合 | A、B 各只有自己的 Region；切换时没有草稿残留。 |
| R1-M03 | 地图编辑 → 数据集图层 | 选择 A 后开始绘制 | 切换 B、锁定 B、解除注册 B | 每次操作立即取消当前草稿；锁定或解除注册后不能把草稿提交到其他数据集。 |
| R1-M04 | 地图编辑 → 数据集图层 | 对 A 改名、隐藏、显示、锁定、拖动排序 | 不保存前观察视口与图层 | 已画 Region 始终留在内存；显隐与顺序即时影响 Overlay；锁定禁止绘制。 |
| R1-M05 | 地图编辑 → 数据集 | A、B 都有 Region 后保存 | 关闭地图，再打开同一 `map.json` | RegionId、顶点和 A/B 归属完全恢复；对应 `region-*.json` 各只含自己的 Feature。 |
| R1-M06 | 区域编辑 → 工具栏 | 在 A 新建 Region | 撤销，再重做；同时观察 Dataset 图层状态 | Region 内容按 Undo/Redo 恢复；Dataset 可见、锁定、顺序不被 History 改写。 |
| R1-M07 | 地图编辑 → 数据集图层 | A、B 各修改后保存 | 制造或观察保存失败提示（不得覆盖有效旧文件） | 保存失败不应留下“一份新、一份旧”的 Region Dataset；修复条件后重开仍为上一稳定状态。 |

验收结论：由用户填写 `PASS / FAIL`；未获得全部 PASS 前，MAP-DATA-A-R1 不得标记 CLOSED，也不得启动 R2。
