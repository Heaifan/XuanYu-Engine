# MAP-DOC-A-R2-F2 · Dataset 真机验收模板

状态：`READY FOR USER RE-ACCEPTANCE`。每项按 IPO 记录实际输入、过程和输出；自动测试不替代真机结论。

| 序号 | 路径 | 输入 | 过程 | 输出 / 判定 |
|---|---|---|---|---|
| F2-M01 | 地图基础 → 数据集 | 新建或打开无 Dataset 的 `map.json` | 切换“数据集”页 | 显示“当前无数据集”和“新建数据集”；记录截图 |
| F2-M02 | 数据集 → 新建数据集 | 类型选择“道路”，不填写 ID（页面无 ID 输入框） | 点击“新建数据集”一次 | 同页列表立即出现一行；主行“道路”，副行 `road-xxxxxx`，状态“正常” |
| F2-M03 | 数据集类型 | 依次选择六类 | 每类创建一项 | UI 显示区域/道路/城镇/资源/河流/地形区域；磁盘 JSON 仍为内部 type |
| F2-M04 | 数据集源文件 | 已存在的候选源文件或重复候选 | 再次创建并观察 | 不覆盖源文件；最多有限尝试后明确失败；列表不增加假行 |
| F2-M05 | 数据集失败路径 | 令 `data` 不可写或 Manifest 提交失败 | 点击“新建数据集” | 显示失败原因；列表保持原状；无孤儿 Dataset |
| F2-M06 | 数据集 → 解除注册 | 选中一行 | 点击“解除注册” | 列表移除；Manifest 移除 descriptor；物理 Dataset 文件保留 |
| F2-M07 | 数据集 → 重开 | 至少两个同 type Dataset | 保存、关闭/重开同一 `map.json` | 两行恢复，ID 不变、状态正常，空态不误显 |
| F2-M08 | 数据集 → 文件核验 | 已创建 Dataset | 打开 Manifest 与 Dataset JSON | Manifest `datasets[].type` 与 JSON `type` 为内部值；UI 中文仅为展示层 |

## R2-M02/M03 补验

原 R2-M02 的手填 `roads` 改按 F2-M02 执行：不再输入 ID，记录自动生成的 `road-xxxxxx`；R2-M03 记录中文主类型、ID 副行和“正常”状态。完成后才可更新 R2 总验收状态，当前不得标记 `CLOSED`。
