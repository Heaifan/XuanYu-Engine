# MAP-DOC-A-R3 · Dataset Layer Editing 真机验收

状态：`USER ACCEPTANCE FAILED → R3-F1 IMPLEMENTING`。真机已确认 R3-M02 Visibility、R3-M03 Lock 通过；R3-M01 左侧行未满宽，R3-M04 拖拽使列表消失。R3 保持 OPEN，以下为 F1 专项复验。

| 序号 | 路径 | 输入 I | 过程 P | 输出 O |
|---|---|---|---|---|
| F1-M01 | 地图编辑器 → 数据集 | 至少两个 Dataset | 观察左侧列表并切换选择 | 每一行和浅蓝选中背景均横向占满左侧列表。 |
| F1-M02 | 地图编辑器 → 数据集 | 选中一个 Dataset | 输入“广东行政区”，点击“应用名称” | 行主信息改为新名称；`ID`、类型、显隐、锁定和顺序不变。 |
| F1-M03 | 文件 → 保存 / 打开 | 已改名 Dataset | 保存并重新打开 | Name 完整恢复；旧 `map.json` 缺少 Name 时仍显示中文类型 fallback。 |
| F1-M04 | 右侧图层 | 至少三个 Dataset | 从“拖动”热区按下并移动超过阈值 | 全部行持续可见；被拖行半透明，目标行上方显示低饱和插入线。 |
| F1-M05 | 右侧图层 / 数据集 | 已完成拖拽排序 | 将末项拖到首项 | 右侧与左侧顺序一致，顺序为连续 0..N-1。 |
| F1-M06 | 右侧图层 | A 已选中，拖动 B；再拖动 A | 依次完成两次排序 | 拖动 B 后仍选中 A；拖动 A 后仍选中 A。 |
| R3-M07 | 文件 → 保存 / 打开 | 已设隐藏、锁定和非默认顺序 | 保存 `map.json`，关闭后重新打开 | 三种 Layer State 与顺序完整恢复。 |
| R3-M08 | 新地图 → 数据集 → 文件 → 保存 | 未保存地图，创建至少两个 Dataset 并改状态/顺序 | 首次保存后重新打开 | Working Storage Promotion 保留 Dataset 与所有 Layer State。 |
