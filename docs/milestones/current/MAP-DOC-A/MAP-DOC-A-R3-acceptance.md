# MAP-DOC-A-R3 · Dataset Layer Editing 真机验收

状态：`READY FOR USER ACCEPTANCE`。自动测试、正式构建和架构门禁通过；以下 IPO 需用户在真机完成。通过前不得标记 `CLOSED`。

| 序号 | 路径 | 输入 I | 过程 P | 输出 O |
|---|---|---|---|---|
| R3-M01 | 地图编辑器 → 数据集 → 图层 | 至少两个 Dataset | 观察右侧图层栏 | 每一行横向占满面板；选中浅蓝背景覆盖整行；类型为主信息、ID 为次信息、状态在右侧。 |
| R3-M02 | 右侧图层 | 选中 A，另有 B | 点击 B 的“隐藏”，再点击“显示” | B 显隐文字与状态切换；A 仍被选中。 |
| R3-M03 | 数据集 → 解除注册 / 右侧图层 | 选中一个 Dataset | 锁定后尝试解除注册，再解锁 | 锁定时解除注册不可用；解锁后恢复可用；锁定不阻止显隐。 |
| R3-M04 | 右侧图层 | 至少三个 Dataset | 从“拖动”热区按下并移动超过小阈值 | 被拖行半透明，目标行上方显示低饱和插入线；普通单击仍仅选择。 |
| R3-M05 | 右侧图层 / 数据集 | 已完成拖拽排序 | 将末项拖到首项 | 右侧与左侧顺序一致，顺序为连续 0..N-1。 |
| R3-M06 | 右侧图层 | A 已选中，拖动 B；再拖动 A | 依次完成两次排序 | 拖动 B 后仍选中 A；拖动 A 后仍选中 A。 |
| R3-M07 | 文件 → 保存 / 打开 | 已设隐藏、锁定和非默认顺序 | 保存 `map.json`，关闭后重新打开 | 三种 Layer State 与顺序完整恢复。 |
| R3-M08 | 新地图 → 数据集 → 文件 → 保存 | 未保存地图，创建至少两个 Dataset 并改状态/顺序 | 首次保存后重新打开 | Working Storage Promotion 保留 Dataset 与所有 Layer State。 |
