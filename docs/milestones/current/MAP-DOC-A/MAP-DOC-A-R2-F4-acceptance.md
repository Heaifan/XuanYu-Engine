# MAP-DOC-A-R2-F4 · 未保存地图工作存储真机验收

状态：`READY FOR USER RE-ACCEPTANCE`，不是 `CLOSED`。

| 序号 | 路径 | 输入 I | 过程 P | 输出 O |
|---|---|---|---|---|
| F4-M01 | 数据集 | 新启动、未保存、类型道路 | 点击“新建数据集” | 不弹保存窗口或提示先保存；列表出现道路与 `road-xxxxxx`。 |
| F4-M02 | 数据集 | 仍未保存 | 依次创建区域、河流、资源 | 全部成功、ID 唯一、列表即时刷新。 |
| F4-M03 | 数据集/图层 | 至少两个 Dataset | 左右两侧分别选择 | 两侧同步，始终只有一个选中项。 |
| F4-M04 | 数据集 | 选中区域 | 点击“解除注册” | 左右投影消失，选择迁移，不要求保存。 |
| F4-M05 | 文件→保存 | 未保存且有多个 Dataset | 选择正式 `map.json` 路径保存 | 保存成功；Dataset、ID、类型和选择均保持。 |
| F4-M06 | 文件系统 | 已完成首次保存 | 检查正式目录 | 仅有 `map.json` 与已注册的 `data/*.json`；Descriptor source 为相对路径。 |
| F4-M07 | 文件→打开 | 已保存地图 | 重开同一 `map.json` | Dataset 数量、ID、中文类型和右侧投影正确。 |
| F4-M08 | 数据集 | 已保存地图 | 创建 Dataset | 沿用原正式路径，不出现 Working Storage 干扰。 |
