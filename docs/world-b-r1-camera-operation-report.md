# WORLD-B-R1 编辑器相机操作实装报告

版本：v0.2.20.2-rz
日期：2026-07-26 13:56:52
分支：feat/WORLD-B-editor-interaction
阶段：WORLD-B-R1：编辑器相机操作闭环

## 裁定

本轮只实现编辑器相机操作，不进入 WarCore、Rotate、Scale、Inspector Transform 或实体 Transform 改造。`ARCH-WORLD-R6` 仍是历史验收记录，当前开发入口保持 WORLD-B。

R1 采用显式唯一观察中心方案：`UiVm.Camera.cs` 持有 `_camera` 与唯一 `_observationCenter`。Frame、Orbit、Pan、Dolly 全部读取和更新同一个观察中心来源，不新增 Camera Service、Camera Entity 或第二套 World 权威。

## 实现变化

- `XuanYu.Editor.Camera` 新增 `CameraFrameResult` 与 `CameraNavigation` 纯算法，负责 Orbit / Pan / Dolly 的数学不变量。
- `EditorCameraFraming` 新增 `FrameAllWithCenter` / `FrameSelectedWithCenter`，保留旧 API 并返回兼容 `CameraState`。
- `UiVm.Camera` 在 Frame All / Frame Selected 时同步更新 `_observationCenter`，Resize 期间若存在相机会话则统一 Cancel。
- `UiVm.CameraNavigation` 新增最小 CameraSession：Begin / Preview / End / Cancel；Cancel 恢复完整起始 `CameraState` 与观察中心，不写 History。
- 视口输入新增 MMB Orbit、Shift+MMB Pan、Wheel Dolly；Win32 NativeHost 与 Avalonia Pointer 两条路径都接入同一 UiVm 相机入口。
- CameraSession 活动期间拒绝 Picking 与 Move Gizmo Begin；Gizmo Capture 活动期间拒绝 Camera Begin 与 Dolly。

## 明确未做

- 未修改 `GlobalWorld`、`EntityRegistry`、实体 Transform、`TransformSession` 或 `EditorHistoryOwner`。
- 未实现 Rotate、Scale、Local、Inspector Transform、WarCore、Soldier、Faction、Organization。
- 未引入新依赖、公共 Camera Service、正交相机、多相机系统或相机书签。

## 自动验证

本轮新增 11 个自动测试，基线 171 增至 182：

- `CameraNavigationTests`：Orbit 保距/保中心/Forward 指向中心、极点保护、Pan 平移不变量、Dolly 比例缩放与非法输入保护。
- `WorldCameraNavigationUiTests`：Frame 更新观察中心、Cancel 恢复起始相机、Gizmo Capture 抢占、旧 PointerUp 不影响新会话、Camera Capture 阻止 Dolly 与 Picking。

当前已完成的完整自动验证：

```text
dotnet build .\XuanYu.Engine.slnx --no-incremental
结果：10 项目，0 Warning / 0 Error

dotnet test .\XuanYu.Engine.slnx --no-build
结果：182 passed / 0 failed / 0 skipped（Core 77 + World 105）

scripts\arch-a-guard.ps1
结果：PASS

5+100
结果：PASS

SVG XML
结果：52/52 PASS

git diff --check
结果：PASS

版本一致性
结果：run.bat / UiWin.axaml / changelog.md / file-tree.md 均为 v0.2.20.2-rz
```

## 真机验收状态

自动测试已覆盖核心数学和会话生命周期，但 R1 尚未宣布真机 CLOSED。仍需用户运行 `run.bat`，按以下中文 IPO 清单逐项记录结果：

#### 测试项目：“聚焦”后围绕选中实体环绕

- 序号：01
- 路径：左侧面板 → 层级 → 选中实体；顶部工具栏 → 视图 → 聚焦；中央视口
- 输入 I：
  - 场景内至少存在一个可见实体。
  - 左侧层级中已选中一个实体。
- 过程 P：
  1. 单击顶部工具栏“视图”区域中的“聚焦”。
  2. 确认中央视口画面聚焦到选中实体附近。
  3. 将鼠标移入中央视口。
  4. 按住鼠标中键左右拖动。
  5. 再按住鼠标中键上下拖动。
  6. 松开鼠标中键。
- 输出 O：
  - 相机围绕选中实体形成的观察中心旋转。
  - 画面不围绕世界原点错误旋转，不翻转，不跳变，无错误日志。

#### 测试项目：“查看全部”后围绕场景中心环绕

- 序号：02
- 路径：顶部工具栏 → 视图 → 查看全部；中央视口
- 输入 I：
  - 场景内存在多个分散实体。
- 过程 P：
  1. 单击顶部工具栏“视图”区域中的“查看全部”。
  2. 确认多个实体进入中央视口可观察范围。
  3. 将鼠标移入中央视口。
  4. 按住鼠标中键左右拖动。
  5. 再按住鼠标中键上下拖动。
  6. 松开鼠标中键。
- 输出 O：
  - 相机围绕全部实体形成的场景中心旋转。
  - 画面不会继续围绕此前“聚焦”的单个实体旋转，无跳变、翻转或错误日志。

#### 测试项目：Shift + 鼠标中键平移视图

- 序号：03
- 路径：中央视口 → Shift + 鼠标中键
- 输入 I：
  - 中央视口内可见至少一个实体。
  - 当前相机已通过“聚焦”或“查看全部”建立稳定观察中心。
- 过程 P：
  1. 将鼠标移入中央视口。
  2. 按住 Shift。
  3. 按住鼠标中键向右拖动。
  4. 保持 Shift 和鼠标中键，再向上拖动。
  5. 松开鼠标中键。
  6. 松开 Shift。
- 输出 O：
  - 画面沿屏幕平面平移。
  - 观察方向和观察距离保持不变，实体大小不因平移突变，无错误日志。

#### 测试项目：滚轮调整观察距离

- 序号：04
- 路径：中央视口 → 鼠标滚轮
- 输入 I：
  - 中央视口内可见至少一个实体。
  - 当前相机已通过“聚焦”或“查看全部”建立稳定观察中心。
- 过程 P：
  1. 将鼠标移入中央视口。
  2. 向前滚动鼠标滚轮。
  3. 观察实体在画面中变大。
  4. 向后滚动鼠标滚轮。
  5. 观察实体在画面中变小。
- 输出 O：
  - 相机只改变到观察中心的距离。
  - 画面焦点不漂移，不穿越焦点，不改变透视角度，不产生错误日志。

#### 测试项目：Escape 取消“环绕”

- 序号：05
- 路径：顶部工具栏 → 视图 → 聚焦；中央视口 → 鼠标中键；键盘 → Escape
- 输入 I：
  - 中央视口内可见已选中实体。
  - 已单击“聚焦”建立观察中心。
- 过程 P：
  1. 将鼠标移入中央视口。
  2. 按住鼠标中键开始拖动，使画面进入旋转过程。
  3. 保持鼠标中键按下时按 Escape。
  4. 松开鼠标中键。
- 输出 O：
  - 当前相机会话被取消。
  - 画面恢复到本次按下鼠标中键前的相机状态，无残留旋转、无错误日志。

#### 测试项目：Escape 取消“平移”

- 序号：06
- 路径：中央视口 → Shift + 鼠标中键；键盘 → Escape
- 输入 I：
  - 中央视口内可见实体。
  - 当前相机已通过“聚焦”或“查看全部”建立稳定观察中心。
- 过程 P：
  1. 将鼠标移入中央视口。
  2. 按住 Shift。
  3. 按住鼠标中键开始拖动，使画面进入平移过程。
  4. 保持鼠标中键按下时按 Escape。
  5. 松开鼠标中键和 Shift。
- 输出 O：
  - 当前相机会话被取消。
  - 画面恢复到本次平移开始前的相机状态，无残留平移、无错误日志。

#### 测试项目：窗口失焦或鼠标捕获丢失时取消相机会话

- 序号：07
- 路径：中央视口 → 鼠标中键；系统窗口 → 切换焦点
- 输入 I：
  - 中央视口内可见实体。
  - 当前相机已通过“聚焦”或“查看全部”建立稳定观察中心。
- 过程 P：
  1. 将鼠标移入中央视口。
  2. 按住鼠标中键开始拖动，使画面进入旋转过程。
  3. 保持鼠标中键按下时切换到其他窗口，或让玄域编辑器窗口失去焦点。
  4. 回到玄域编辑器窗口。
  5. 松开鼠标中键。
- 输出 O：
  - 当前相机会话被取消。
  - 回到窗口后不会继续拖拽旋转，不出现捕获残留、卡住或错误日志。

#### 测试项目：调整窗口大小后保持相机焦点

- 序号：08
- 路径：顶部工具栏 → 视图 → 聚焦；系统窗口边框 → 调整大小；中央视口
- 输入 I：
  - 中央视口内可见已选中实体。
  - 已单击“聚焦”建立观察中心。
- 过程 P：
  1. 观察并记住选中实体在画面中的位置。
  2. 拖动窗口边框调整玄域编辑器窗口大小。
  3. 等待画面刷新稳定。
  4. 将鼠标移入中央视口。
  5. 按住鼠标中键左右拖动。
- 输出 O：
  - 调整窗口大小后，选中实体仍围绕同一个观察中心可见。
  - 后续旋转仍围绕该实体附近，不漂移到世界原点，无错误日志。

#### 测试项目：相机操作期间不改变选择

- 序号：09
- 路径：左侧面板 → 层级 → 选中实体；中央视口 → 鼠标中键
- 输入 I：
  - 场景内存在至少两个实体。
  - 左侧层级中已选中其中一个实体。
- 过程 P：
  1. 确认左侧层级当前选中实体。
  2. 将鼠标移入中央视口。
  3. 按住鼠标中键拖动旋转画面。
  4. 松开鼠标中键。
  5. 再按住 Shift + 鼠标中键拖动平移画面。
  6. 松开鼠标中键和 Shift。
- 输出 O：
  - 左侧层级的选中实体保持不变。
  - 相机旋转和平移不会触发实体选择切换，不产生 Picking 误选日志。

#### 测试项目：实体“移动”与相机输入互斥

- 序号：10
- 路径：顶部工具栏 → 变换 → 移动；中央视口 → 移动轴；中央视口 → 鼠标中键
- 输入 I：
  - 场景内存在已选中实体。
  - 顶部工具栏已切换到“移动”。
  - 中央视口中可见移动轴。
- 过程 P：
  1. 在中央视口按住移动轴并拖动，确认实体进入移动预览。
  2. 移动预览过程中尝试按下鼠标中键。
  3. 松开鼠标中键。
  4. 松开移动轴拖动。
  5. 再单独按住鼠标中键拖动视图。
- 输出 O：
  - 实体移动预览期间不会同时进入相机旋转或平移。
  - 单独使用鼠标中键时才进入相机操作，两类会话不会同时预览。

#### 测试项目：相机会话中调整窗口大小

- 序号：11
- 路径：中央视口 → 鼠标中键；系统窗口边框 → 调整大小
- 输入 I：
  - 中央视口内可见实体。
  - 当前相机已通过“聚焦”或“查看全部”建立稳定观察中心。
- 过程 P：
  1. 将鼠标移入中央视口。
  2. 按住鼠标中键开始拖动，使画面进入旋转过程。
  3. 保持相机会话过程中调整窗口大小。
  4. 松开鼠标中键。
  5. 再次按住鼠标中键拖动视图。
- 输出 O：
  - 调整窗口大小会取消当前相机会话，或在刷新后保持稳定相机状态。
  - 后续重新按下鼠标中键才能开始新的相机操作，不出现 NaN、黑屏、跳变或错误日志。

## 下一步

真机验收通过后补 `docs(editor): close WORLD-B R1 camera acceptance` 收口文档和 SVG，再普通 push。若真机发现代码错误，只修 R1 相机问题，不进入 R2、Rotate、Scale 或 WarCore。
