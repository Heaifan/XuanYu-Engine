# WAVE-2.5-D1 Camera/Picking/Gizmo Migration Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make Camera, Picking, and Gizmo concrete consumers of the frozen viewport input router and lifecycle, with deterministic owner arbitration and cancellation coverage.

**Architecture:** Keep `EditorPointerEvent` as the only platform-neutral input boundary. Register exactly one consumer for each D1 owner (`Camera`, `Picking`, `Gizmo`) in a D1 consumer set. The router remains the only dispatcher; `ViewportGestureLifecycle` remains the only owner/capture/cancel state machine. Consumer callbacks invoke existing domain commands through injected operations, so this task does not alter D2 consumers, renderer code, NativeControlHost, or the existing production Host route.

**Tech Stack:** C#, xUnit, PowerShell architecture gates, Git worktree.

## Frozen Scope

- Start from `00d8f147b1ffda5eabab2ae3a308f32c00b16258`.
- Implement only D1 consumer adapters, arbitration policy, focused tests, and report/entry-point documentation.
- Do not modify Camera/Picking/Gizmo domain behavior, D2 consumers, renderer, composition, NativeHost, Win32 host, or C GAPs.
- Keep every handwritten `.cs`, `.axaml`, and `.js` file at or below 100 lines.

## Task 1: Establish the D1 contract with failing tests

**Files:**

- Create focused D1 consumer contract tests under `XuanYu.World.Tests/Viewport/InputIntegration/`.
- Add only test helpers needed to express real `EditorPointerEvent` dispatch and lifecycle observations.

**Steps:**

1. Test Camera, Picking, and Gizmo registration and unique `GestureOwner` values.
2. Test priority: Gizmo hit claims before Camera/Picking; Camera gesture claims before click Picking; Picking claims a click only when no higher-priority consumer claims it.
3. Test active-owner exclusivity, release/commit, Escape/CaptureLost/FocusLost/WindowDeactivate cancellation, repeated cancel, and next-gesture restart.
4. Run the focused tests and record the expected RED failure before adding the consumer set.

## Task 2: Implement the minimal D1 consumer set

**Files:**

- Create small production files under `XuanYu.Editor/Input/Consumers/` for the D1 consumer contracts and arbitration composition.
- Modify only existing router-facing abstractions if a directly tested contract requires it.

**Steps:**

1. Implement Camera, Picking, and Gizmo consumers with one owner each.
2. Keep hit-test/gesture eligibility in the D1 arbitration composition; consumers must not privately compete after Router selection.
3. Route Begin/Update/Commit/Cancel through the existing lifecycle callbacks and injected domain operations.
4. Re-run focused tests GREEN, then refactor only without changing behavior.

## Task 3: Document migration boundary and regression entry

**Files:**

- Create a D1 migration report under `docs/`.
- Add or extend a repository script only if the existing baseline entry can include the D1 focused suite without changing production behavior.

**Steps:**

1. Record the current Host direct-route boundary and the D1 consumer contract now available.
2. Record all six C GAPs and state that none blocks D1 unless tests prove otherwise.
3. State explicitly that D2, production route switching, Viewport migration, Vulkan, and WAVE-3 remain untouched.

## Task 4: Verification and delivery

Run serially: D1 focused tests, Phase-1 tests, Router/Cancellation/Platform Parity tests, Viewport Migration Baseline, Solution Build, ARCH-A, ARCH-VIEWPORT, 5+100, and `git diff --check`. Confirm the known baseline remains 22 and New Regression is 0. Commit, push the requested branch, and verify local/remote equality.
