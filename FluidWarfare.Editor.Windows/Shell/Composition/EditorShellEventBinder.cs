namespace FluidWarfare.Editor.Windows.Shell.Composition;

// Empty — event binding kept in EditorShell.SubscribePanelEvents
// due to private handler accessibility. May be revived when handlers
// are migrated into dedicated routes.
public static class EditorShellEventBinder { }
