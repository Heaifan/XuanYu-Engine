# MAP-REGION-SNAP-R1-REVALIDATE 问题与验收需求单

## 目标

在当前主开发基线下完成区域顶点吸附、闭合、显示和几何顶点编辑的可验收闭环。范围限定为 R1 Vertex → Vertex，不进入 R2 Vertex → Edge。

## 用户指出的问题

1. 用户只绘制 5 个顶点，但界面曾显示 6 个。
2. 区域绘制过程中，靠近首点准备闭合时没有可靠的自动闭合锚点。
3. 未完成区域时看到空白，无法判断当前是否存在可吸附目标。
4. 吸附按钮曾显示关闭，区域吸附行为与界面状态不一致。
5. 区域合并/完成后不显示对应顶点。
6. 选中区域后无法拖动对应顶点。
7. 顶部绘制工具栏曾出现无意义的空白占位。

## 已进行的尝试与结果

### 1. 重复顶点计数

- 根因：草稿阶段无条件追加与上一个点相同的坐标。
- 修复：拒绝相邻重复草稿顶点。
- 结果：区域状态、区域绘制和吸附专项通过。

### 2. 吸附状态

- 根因：默认吸附为关闭，区域绘制解析没有读取总开关。
- 修复：默认开启；关闭后真正返回 Raw Pointer；Alt 仍可临时取消。
- 结果：吸附专项通过。

### 3. 首点闭合

- 根因：闭合候选只在 Pointer Move 中计算，直接点击首点时没有进入候选状态。
- 修复：Pointer Pressed 时补做首点屏幕距离判断，命中后直接闭合。
- 结果：新增直接点击首点回归测试。

### 4. 顶点显示与几何入口

- 根因：MapSession 的区域选择事件会清空 `_selectedMapGeometry`，导致渲染不画顶点、检查器不显示几何编辑入口。
- 修复：区域选择同步为几何选择状态。
- 结果：新增区域选择同步测试。

### 5. 顶点拖动

- 根因：已选中区域的 Pointer Down 仍被 `IsGeometryEditingActive` 门槛拦截。
- 修复：已选中区域时，直接按住顶点即可启动拖动；点区域内部仍保持选择行为。
- 结果：新增“选中区域顶点可直接开始拖动”测试，PASS。

## 验收标准

- 已提交区域显示填色、闭合边界和顶点标记。
- 选中区域后右侧出现“编辑几何”。
- 鼠标按住任一顶点并移动，顶点和边实时跟随。
- 松开鼠标后坐标提交；Escape 取消后恢复原坐标。
- 关闭吸附时拖动使用原始指针坐标；开启吸附时保持 R1 顶点吸附。
- 首点附近直接点击即可闭合，不新增首尾重复顶点。
- 保存、重载后顶点坐标保持一致。

## 当前状态

- 代码修复：已完成。
- 顶点直接拖动回归：PASS。
- 专项：12/12 PASS。
- World.Tests Build：0 warnings / 0 errors。
- 人工 `run.bat`：PASS（用户已确认相邻区域、Alt、Undo/Redo、保存操作正常）。
- R1 状态：FREEZE APPROVED。

## 待人工交叉验收

- Navigation Gizmo 区：Navigation 优先取得手势。
- 区域顶点：MapEdit 取得拖动手势并实时预览。
- 区域内部：只选择，不开始 Vertex Drag。
- 地图空白：不产生错误顶点拖动。
- 普通 Viewport Picking：在对应模式下不被无理由吞掉。
- 关闭/开启吸附、Escape 回滚、Save/Reload 坐标保持：已由用户完成正式入口验收。

## 最终结论

```text
MAP-REGION-SNAP-R1
Automated Gate: PASS
Solution Build: PASS · 0W / 0E
User Acceptance: PASS
R1 FREEZE: APPROVED
```
