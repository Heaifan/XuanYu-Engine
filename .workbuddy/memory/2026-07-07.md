# 2026-07-07 工作日志

## 19:16 — 项目熟悉（docs 全套）
- 通读 `docs/` 全部 37 个文件（36 .md + 1 .log），建立项目心智模型。
- 输出结构化简报：身份定位、四层架构、编码宪法、里程碑地图（9.0X/9.0Y/9.0C 封板，9.1A 冻结，9.1B/9.1C + Vulkan re-entry 活跃）、两条横切约束（Preview/Commit、诊断安全）。
- 发现文档三方命名空间矛盾：`NAMING_RULES`/`AI_DEVELOPMENT_RULES` 写 `FluidWarfare.*`；`naming-XuanYu-Engine.md` 目标 `XuanYu.Engine.*`；实际代码 `XuanYu.*`。以实际代码为准。
- 长期速查已写入 `MEMORY.md`。

## 19:24 — Git 同步覆盖
- 仓库 origin: `https://github.com/Heaifan/XuanYu-Engine.git`，分支 `fix/RZ-Fix2-ui-baseline`。
- 本地落后远端 6 个提交、0 独有；执行 `git reset --hard origin/fix/RZ-Fix2-ui-baseline`，对齐到 `656ec45`。
- 工作区仅 `.workbuddy/`（记忆目录）未跟踪，已保留；未执行 `git clean`，故记忆未丢失。
- 远端新增分支：`main`、`backup/pre-transform-rewrite`、`codex/transform-clean-rewrite`；新增 tag `m2.6.1-prefix-proof`（FYI，未切换）。

## 19:26 — VK3-A 实装（Render.Abstractions 契约层）
- 用户决策：VK3 可进入，但拆小步（A/B/C）；Instance 归属交我定（建议 VK3-B 新建 VulkanInstanceOwner）。
- 核现状缺口：原本无 `XuanYu.Render.Abstractions` 工程；`VulkanInstanceOwner` 不存在（探针 VulkanClearSession 自行 CreateInstance）；UI 已引用 Silk.NET（探针所致，红线=不得新增）。
- 新建 `XuanYu.Render.Abstractions` 工程（net10.0，无 Silk.NET），含 `NativeHostSurfaceHandle`（Hwnd/Hinstance/Width/Height/DpiScale）与 `INativeHostSurfaceBridge`（Attach/Resize/Detach）。
- Editor.UI 加工程引用；新增 `NativeHostSurfaceContract.cs` 把 `NativeHostHandleSnapshot` 映射为交接句柄（取 `Win32ViewportHost.ModuleHandle` 作 Hinstance）。
- 构建：Abstractions 0W0E；Editor.UI 低内存模式 0W0E（沙箱直接 build 会因 MSBuild OOM 崩溃，非代码问题）。
- 提交到新分支 `fix/RZ-VK3-A-surface-contract`，commit `2bfbe2e`（5 文件，`.workbuddy/` 未入库），未推送远端。
- 红线守住：VK3-A 无 Swapchain/Device/Queue/PhysicalDevice/LogicalDevice，UI 未新增 Silk.NET 使用点。
