# 2026-07-08 工作日志

## 拉取远端最新进度（git pull --ff-only）
- 当前分支：`fix/RZ-VK3-A-surface-contract`，快进合并 `d441f1c..e968b1e`（无本地冲突，仅 `.workbuddy/` 未跟踪）。
- 远端合入内容：
  - VK3 全链路收口（VK3-B1/B2/C1/C2/C2-R1）：新增 `VulkanInstanceOwner`、`VulkanSurfaceOwner`、`VulkanNativeHostSurfaceBridge`、`VulkanSurfaceBridgeProvider`、`VulkanInstanceCreateInfoBuilder`、`VulkanInstanceExtensions`、各 `*LogFormatter`/`*Result` 等。
  - 文档：`docs/rz-vk3-closure.md`（VK3 验收收口）、`docs/rz-vk4-plan.md`（VK4 规划）、`changelog.md` +364 行、`file-tree.md` 更新。
- 结论：VK3 已通过验收，NativeHost HWND 生命周期已接入 Vulkan Instance + Surface，但 Swapchain/渲染未做，黑屏为预期；VK4 负责真正出画面（Device/Swapchain/渲染循环）。
- 已同步更新 MEMORY.md 里程碑状态。

## 实装 VK4-A 物理设备选择链路（审计边界压死）
- 用户审计结论：VK3 收口合格、VK4 进入 A 阶段；边界压死——VK4-A 只做 PhysicalDevice 选择，不碰 LogicalDevice/Queue/Swapchain/清屏。
- 新增 3 文件（`XuanYu.Render.Vulkan/Device/`）：`VulkanPhysicalDeviceInfo.cs`(12)、`VulkanQueueFamilySelection.cs`(14)、`VulkanPhysicalDeviceSelector.cs`(99，含结果 record)。选择器枚举设备、检查 Graphics/Present 队列族与 Surface 呈现支持、优先独显、返回纯数据、中文日志。
- 修改 `VulkanNativeHostSurfaceBridge.cs`(93→110)：`Attach` 后调用 `RunDeviceSelection()`，异常仅记日志不影响 VK3 契约；`Resize` 不重建 Surface 不变。
- 构建：Render.Vulkan 与 Editor.UI 均 0W0E（低内存构建）。
- 提交：代码 `79eabd0c5f11c88ab78607041395353cf05156ae`，文档 `adefae6777043c9ea4ef427c36bd4cf91ab5eb0a`，已 push 到 origin `fix/RZ-VK3-A-surface-contract`（e968b1e..adefae6）。
- 红线全部守住：无 VkDevice/Queue/Swapchain 实装；Editor.UI 未新增 Silk.NET.Vulkan 引用；未复制旧探针。
- 下一步 VK4-A-R1（审计+日志补强），严禁顺手推进 VK4-B。

## VK4-A-R1 收口修正（压回 100 行红线）
- 用户审计结论：VK4-A 功能方向合格，但 `VulkanNativeHostSurfaceBridge.cs` 93→110 行越过 100 行红线，不能判收口。必须先做 R1 压回再进 VK4-B。
- 新增 `XuanYu.Render.Vulkan/Bridge/VulkanBridgePhysicalDeviceAttachStep.cs`(23)：把 Attach 后的 PhysicalDevice 选择与中文日志整体迁出，仅委托调用 `VulkanPhysicalDeviceSelector.Select` + try/catch 记日志。
- 修改 `VulkanNativeHostSurfaceBridge.cs`(110→96)：删除内联 `RunDeviceSelection()`，改在 `Attach` 末尾带引用空值守卫地委托给新 step；`using Device` 换 `using Bridge`；Bridge 仅保留生命周期编排。
- 构建：Render.Vulkan 与 Editor.UI 均 0W0E（低内存构建）。
- 提交：代码 `fffb6d1a006306f3051ac8cabc2fa27a977301ec`，文档 `912f7fb7076f710d011e47efb58f0a960326e8cb`，已 push（adefae6..912f7fb）。
- 红线校验：grep 确认无 VkDevice/Queue/Swapchain/ClearFrame 新增实装（仅注释/日志）；Editor.UI 仍无新 Silk.NET.Vulkan 类型引用。
- 结论：VK4-A 边界与行数红线均守住，正式收口。下一步 VK4-B（LogicalDevice+Queue），严禁顺手推进 C/D。

## 用户截图审计 VK4-A-R1 验收结论（非阻塞项记录）
- 审计口径：除「测试机型号不是 RTX 3060、而是备用机 RTX 3050 Laptop」外，截图无阻塞 VK4-B 的硬伤；但有 3 个非阻塞问题须记下。
- **问题1（日志易误判）**：截图同时存在旧探针日志 `【Vulkan探针】` 与正式链路日志 `【VulkanBridge】`/`【VulkanDevice】`。旧探针不参与正式生命周期，报告须显式区分两者，避免误判旧探针参与了 VK3/VK4 选择。
- **问题2（候选设备含杂质）**：枚举出 9 个候选，含真实设备（RTX 3050 Laptop / Intel Iris Xe）、D3D12 wrapper（`Microsoft Direct3D12 (...)`）、`Microsoft Basic Render Driver`。目前正确命中真实独显；**VK4-B 创建 LogicalDevice 必须基于 VK4-A 最终选择结果，不得重新枚举后选错设备**（尤其不得选 D3D12 wrapper / Basic Render / iGPU）。
- **问题3（日志截断）**：UI 面板省略号截断关键内容，影响审计但不影响功能；后续可做「日志详情」面板，非 VK4-B 前置。
- **确认项**：Resize 日志 `尺寸变化已接收：不重建 Surface` 正常（VK3 红线仍在）；当前黑屏为预期（无 Swapchain/ImageView/RenderPass/CommandBuffer/Present，看到颜色要等 VK4-D）。
- **结论**：3 个非阻塞问题均不阻塞 VK4-B；可进入 VK4-B，但指令须加硬约束「VK4-B 必须基于 VK4-A 最终选择结果创建 LogicalDevice，不得重新枚举后自行选择其他设备」。
- 已落盘：docs/rz-vk4-plan.md（测试机型号订正 + 新增「VK4-A 验收后审计备注」小节含 VK4-B 硬约束）、MEMORY.md（新增「VK4 审计订正」速查段）。

## 实装 VK4-B（基于 VK4-A 选择结果创建 LogicalDevice + 队列）
- 用户拍板走 A（直接开 VK4-B，跳过重复 8 条人工清单）。并纠偏：不得把「RTX 3050 固定口径」写死成长期硬规则，真正规则是「认 VK4-A 最终选择结果」；已同步订正 docs/rz-vk4-plan.md 与 MEMORY.md 口径。
- 新增 3 文件：`Device/VulkanPhysicalDeviceSelection.cs`(12，从选择器抽出选择结果记录并补 PhysicalDevice 句柄字段)、`Device/VulkanDeviceOwner.cs`(96，基于选择结果创建 VkDevice + Graphics/Present 队列、Dispose 幂等)、`Bridge/VulkanBridgeDeviceAttachStep.cs`(29，链式驱动创建设备)。
- 修改 3 文件：`Device/VulkanPhysicalDeviceSelector.cs`(99→93，移除内联记录、捕获 bestDevice 随结果返回句柄)、`Bridge/VulkanBridgePhysicalDeviceAttachStep.cs`(23→24，Run 返回选择结果)、`VulkanNativeHostSurfaceBridge.cs`(96→98，Attach 串「选择→创建设备」、Detach 逆序 Device→Surface→Instance、Resize 不变)。
- 构建：Render.Vulkan 与 Editor.UI 均 0W0E（低内存构建；中途因 PID 26356 旧编辑器进程锁 bin 导致拷贝失败，已 taskkill 释放后重建通过）。
- 提交：代码 `21f24026ff7b102c12b8346563c066b8a64449a7`，文档 `1f5da30216dbe11286660d3570b7e42cbc14270e`，已 push 到 origin `fix/RZ-VK3-A-surface-contract`（95ccae9..1f5da30）。
- 红线校验：grep 确认无 Swapchain/ImageView/RenderPass/CommandBuffer/Clear/CreateSwapchain 新增实装（仅注释/日志）；Editor.UI 仍无新 Silk.NET.Vulkan 类型引用（仅历史探针债 VulkanClearSession.*.cs 与 csproj ProjectReference，未触碰）；所有 .cs ≤100 行。
- 结论：VK4-B 边界与行数红线均守住，可判功能收口。下一步必须做 VK4-B-R1（生命周期审计，重点 Detach 顺序 Device→Surface→Instance 与异常路径资源不泄漏），严禁顺手推进 VK4-C。

## VK4-B-R1 生命周期审计与运行验证（静态审计通过；运行时待用户机）
- 用户确认 VK4-B 代码边界合格，进入 VK4-B-R1；明确不进 VK4-C，先验生命周期。
- 静态审计全绿（无需用户机即可判定）：
  - Detach 逆序释放 `LogicalDevice → Surface → Instance`（VulkanNativeHostSurfaceBridge.cs 78–80 行）。
  - Attach 异常路径逆序回滚 surface/instance/_vk（catch 51–56 行）；设备 step 失败仅返回 null 不抛，已挂 Instance+Surface 仍保留（预期部分成功，无泄漏）。
  - 未重枚举：VulkanDeviceOwner.Create 复用 sel.Handle，不重枚举。
  - 红线 grep：Swapchain/ImageView/RenderPass/CommandPool/CommandBuffer 仅出现在注释；Clear/Present 实装 NONE_MATCH。
  - UI(Editor.UI) 未新增 Silk.NET.Vulkan 引用：仅历史探针债 VulkanClearSession*.cs（4 文件）+ 生成 obj/bin；csproj ProjectReference 历史债未动。
  - 命名约定一致：VulkanDeviceOwner 用 `using VulkanDevice = Silk.NET.Vulkan.Device;` 别名，属性名 `LogicalDevice`，无 Device 作属性名。
  - 全 .cs ≤100 行：Bridge 98 / DeviceOwner 96 / SurfaceOwner 75 / InstanceOwner 57 / 其余 9–29。
  - 两项目 0W0E：Render.Vulkan（3.9s）、Editor.UI（10.6s）。
- 行数订正：用户口头「DeviceOwner 95 行」实测为 96 行（余 4 行），以 wc 为准。
- 风险点移交 VK4-C：Bridge 仅余 2 行、DeviceOwner 仅余 4 行，均禁止再膨胀；命名口径锁定 VulkanDevice 别名 / VulkanDeviceOwner / LogicalDevice。
- changelog.md 新增 `[VK4-B-R1]` 章节（静态审计结果 + 行数明细 + 风险点 + 待用户机 12 项清单）；file-tree.md 无新文件故未动。
- 待用户机器跑 12 项后核对；全过则 VK4-B 完全收口、再议 VK4-C（Swapchain 独立 owner/step）。

## VK4-B-R1 运行时验证（用户截图 5 张，11/12 通过）
- 用户在备用机（RTX 3050 4GB Laptop GPU）运行编辑器并贴回 5 张日志截图。
- **通过 11 项**：
  - ① 启动无崩溃（状态栏「就绪」，错误=0）
  - ② Instance+Surface 已创建（【VulkanBridge】附加成功；窗口句柄：0x1809E4）
  - ③ 枚举物理设备（候选数量=9，含 D3D12 wrapper / Basic Render Driver 杂质）
  - ④ 选择本机独显（NVIDIA GeForce RTX 3050 4GB Laptop GPU；原因: 优先独显）— 复用 VK4-A 结果 ✅
  - ⑤ 开始创建 LogicalDevice（物理设备名正确传递）
  - ⑥ Graphics 队列族=0
  - ⑦ Present 队列族=0（与 Graphics 同族，NVIDIA 典型行为）
  - ⑧ LogicalDevice 创建成功
  - ⑨ Queue 获取成功（Graphics + Present）
  - ⑩ Resize 不重建 Surface/Device/Queue（多次「不重建 Surface」日志确认，尺寸从 713×549 变到 713×188）
  - ⑫ 黑屏、无 Swapchain/Clear/Present 日志（NativeHost Probe 全黑，零实装命中）
- **待验证 1 项**：
  - ⑪ 关闭编辑器的 Detach 释放顺序 LogicalDevice→Surface→Instance — 截图时编辑器仍在运行，需关闭后看日志
- 关键观察：
  - 旧探针 `【Vulkan探针】` 与正式链路 `【VulkanBridge】`/`【VulkanDevice】` 日志通道清晰分离 ✅
  - UI 状态栏仍显示「Device / Swapchain 尚未接入」— 属 UI 层旧文案，非 VK4-B 链路问题
- 结论：VK4-B 运行时链路完整正确，仅差关闭时的 Detach 顺序验证即可宣布完全收口。

## LOG-UX-1 日志多选复制与详情换行修复（仅 UI，不进 VK4-C）
- 用户拍板在 VK4-B-R1 期间插一轮极小 `LOG-UX-1`：只修日志面板交互，服务 Vulkan 生命周期日志复制审计，不碰 Vulkan/NativeHost/Render.Vulkan。
- 改动（全部 XuanYu.Editor.UI，Render.Vulkan 零改动）：
  - `Foot.axaml`：ListBox 改 `SelectionMode=Multiple`(x:Static)、加 `x:Name=LogList`、`SelectionChanged`/`KeyUp` 事件；根加 `xmlns:av="using:Avalonia.Controls"`。
  - `Foot.axaml.cs`(11→34)：`LogList_SelectionChanged` 把选中 `LogEntry[]` 推 VM；`LogList_KeyUp` 处理 `Ctrl+A`(SelectAll 当前筛选结果) 与 `Ctrl+C`(写剪贴板)。
  - `UiVm.Logging.cs`(88→100)：新增 `_selectedEntries`/`SetSelectedEntries`/`HasSelectedEntries`/`SelectedEntriesClipboardText`（委托 FromMany）。**已到 100 行红线，后续不能再往该文件加职责**。
  - `EditorLogClipboardText.cs`(15→25)：新增 `FromMany` 输出纯文本表格（表头 + 每行 \t 分隔，详情不截断）。
  - `LogDetailPanel.axaml`(63→64)：`detailBody` 样式补 `TextWrapping=Wrap` + `AcceptsReturn=True`，详情不再横向截断；保留「复制详情」按钮。
- 构建：Editor.UI 0W0E。file-tree.md 因无新增/移动文件未动。
- 关键陷阱：Avalonia 12.0.4 的 `SelectionMode` 枚举**无 `Extended` 成员**（旧版才有），XAML `SelectionMode="Extended"` 触发 AVLN3000；旧 Extended 语义（单击选一行/Shift 范围/Ctrl 切换）在 12.0.4 由 `Multiple` 提供；须用 `x:Static av:SelectionMode.Multiple`。`Multiple` 普通单击是切换该行（非只选一行），但详情面板按最后点击项显示，不影响审计复制。
- 提交待 push（代码 + changelog）。下一步：用户重跑编辑器 Ctrl+A/Ctrl+C 贴日志，核对 VK4-B-R1 最后一项 Detach 顺序。

## LOG-UX-1-R1 Ctrl+C 复制无响应修复（用户反馈）
- 用户实测 Ctrl+C 无反应。根因：`Foot.axaml` 用 `KeyUp` + `e.KeyModifiers.HasFlag(Control)` 判断快捷键——Avalonia 的 `KeyUp.KeyModifiers` 反映松开键那一刻状态，先松 Ctrl 再松 C 时条件不满足被跳过；原 `async void`+`await SetTextAsync` 也无异常兜底。
- 修复（仅 XuanYu.Editor.UI）：
  - `Foot.axaml`：`KeyUp` → `KeyDown`（按下 C 瞬间 Ctrl 必仍按着，检测稳定）。
  - `Foot.axaml.cs`(34→36)：`LogList_KeyDown`；`Ctrl+A`→SelectAll，`Ctrl+C`→`TopLevel.Clipboard.SetTextAsync` 包 `try/catch`（失败 Debug.WriteLine 不崩）。
  - `UiVm.Logging.cs`(100→96)：把 11 行 `RefreshLogBindings` 压成单行腾空间，新增 `NotifyLogCopied()`（复制成功后写「已复制 N 条日志到剪贴板」信息日志，提供可见反馈）。96 行未破红线。
- 构建 Editor.UI 0W0E；Foot.axaml.cs 36 / UiVm.Logging.cs 96 / Foot.axaml 95，均 ≤100；Render.Vulkan 零改动。
- 提交 `LOG-UX-1-R1` 修复（代码 + changelog.md `[LOG-UX-1-R1]` 章节），push 到 origin。下一步：用户重测 Ctrl+C（面板出现「已复制 N 条」反馈 + 记事本粘贴为纯文本表格），随后关编辑器核对 VK4-B-R1 第⑪项 Detach 顺序。

## LOG-UX-2 会话日志落盘（解决“关闭后拿不到 Detach 日志”）
- 用户纠偏：VK4-B-R1 第⑪项（关闭时 Detach 顺序 Device→Surface→Instance）无法靠“关闭后 Ctrl+C”验证，因为窗口关了 UI 面板也没了。给三方案：A 控制台 / B 落盘 / C NativeHost 重建按钮（不推荐）。决策树：先 A，不行就 B。
- 代码核查：grep 确认 Editor.UI 无 Console/Trace/File 日志输出；`EditorLogBus` 仅 `buffer.Add(...)`，无 console sink → 方案 A 在代码层面不成立。按决策树直接开 B。
- 实现（仅 XuanYu.Editor.UI，Render.Vulkan/ NativeHost 零改动）：
  - `EditorLogBus.cs`(21→44)：加 `_logDir`/`_logPath`（相对 `Environment.CurrentDirectory/logs`，每次启动重建 `editor-session-latest.log` 只留最近会话）；`Write` 内 `buffer.Add` 后 `AppendToFile`，格式 `时间\t级别\t来源\t分类\t消息\t详情`（与剪贴板一致）；包 try/catch，落盘失败不阻塞 UI（诊断安全约定）。
  - `.gitignore` 加 `logs/` 避免会话日志入仓。
- 构建 Editor.UI 0W0E；EditorLogBus.cs 44 行 ≤100；另：构建时遇 PID 24876 旧编辑器进程锁 bin DLL（MSB3026/3027），taskkill 释放后重建通过（非代码错误）。
- 提交 `LOG-UX-2`（代码 + .gitignore + changelog.md `[LOG-UX-2]` 章节），push `bc7f60d`。
- 用户重测路径：run.bat 启动→操作→关闭→打开 `logs/editor-session-latest.log` grep `LogicalDevice 释放成功`/`Surface 已销毁`/`Instance 已销毁` 确认 Device→Surface→Instance 顺序。全过则 VK4-B 完全收口、再议 VK4-C。
- **更正（用户实测后）**：方案 A 其实能行——`VulkanSurfaceOwner.Dispose`/`VulkanInstanceOwner.Dispose` 内部用 `Console.WriteLine` **直写控制台**（绕过 EditorLogBus），控制台本就有 Surface/Instance 释放行；当初只查 EditorLogBus 判定“方案 A 不成立”是片面的。选 方案 B（文件）仍正确（持久），但它只收 `Emit` 路由的日志。

## VK4-B-R1 日志路由补强（第⑪项证据闭合）
- 用户贴回 `editor-session-latest.log` 末尾两行：`【VulkanDevice】LogicalDevice 释放成功` + `【VulkanBridge】分离完成：Surface + Instance 已释放`，**缺 Surface/Instance 各自释放行**。
- 根因：`VulkanSurfaceOwner.Dispose`(73行)/`VulkanInstanceOwner.Dispose`(55行) 用 `Console.WriteLine` 直写控制台、不经 `_log` 回调 → 不入 UI 缓冲 → 不被 LOG-UX-2 落盘；Bridge 只发合并行。释放顺序代码本就正确（Device→Surface→Instance），仅文件证据缺独立行。
- 修复（仅日志，不改 Vulkan 语义/释放顺序，不碰 NativeHost/不进 VK4-C）：
  - `VulkanBridgeLogFormatter.cs`(23→29)：新增 `SurfaceDisposed()`→`【VulkanBridge】Surface 已释放`、`InstanceDisposed()`→`【VulkanBridge】Instance 已销毁`。
  - `VulkanNativeHostSurfaceBridge.cs`(98→**100**)：Detach 在 ②Surface、③Instance 释放后各补一条 `Emit`（98→100，压红线边界）。
  - 现文件出现独立行：`LogicalDevice 释放成功` → `Surface 已释放` → `Instance 已销毁` → `分离完成：Surface + Instance 已释放`。
- 构建 Render.Vulkan 0W0E；提交 `c4c804a`（代码）+ `c5a9524`（changelog 关联章节），均已 push origin。
- 结论：第⑪项证据闭合，VK4-B 可宣布完全收口。下一步：用户重跑编辑器、关窗、贴 `logs/editor-session-latest.log` 的 Detach 段确认三行顺序 Device→Surface→Instance；全过则正式收口、再议 VK4-C（Swapchain 独立 owner/step）。

## LOG-UX-2 回退（VK4-B 完全收口后清理临时调试手段）
- 用户贴回关闭后日志，Detach 顺序确证：`LogicalDevice 释放成功`(22:14:21) → `Surface 已释放` → `Instance 已销毁` → `分离完成`。**VK4-B 正式完全收口**（VK4-A / VK4-A-R1 / VK4-B / VK4-B-R1 全通过）。
- 用户要求删除 LOG-UX-2 的 `logs/editor-session-latest.log` 自动生成功能（仅临时调试，不要增加文件数/占用）。回退动作：
  - `EditorLogBus.cs`(44→21)：还原为纯内存版，移除 `System.IO` 依赖与文件追加逻辑。
  - `.gitignore` 移除 `logs/` 条目。
  - 磁盘 `logs/` 目录已 `rm -rf` 删除。
  - 不碰 Vulkan / NativeHost；Editor.UI 构建 0W0E。
- changelog `LOG-UX-2` 章节加「回退」说明（临时调试，收口后移除）。提交待 push。
- **当前状态**：LOG-UX-1 通过 / LOG-UX-2 已回退 / VK4-B（含 R1）完全收口 / VK4-C 暂缓。

## VK4-C-Plan Swapchain 生命周期规划（仅文档，不实装）
- 用户确认 VK4-B 正式收口、不需 R2；要求先开 VK4-C-Plan 压死边界，不直接实装 VK4-C（Swapchain 易回潮 Resize 生命周期）。
- 新增 `docs/rz-vk4-c-swapchain-plan.md`：只规划 Swapchain + Images + ImageViews 生命周期。
  - 目标：只建 VkSwapchainKHR + Images + ImageViews；不建 RenderPass/Framebuffer/CommandPool/CommandBuffer、不 Clear/Present（仍黑屏为预期）。
  - Dispose 顺序硬约束：`ImageViews → Swapchain → LogicalDevice → Surface → Instance`。
  - Resize 只重建 Swapchain + ImageViews；禁止重建 Surface/Instance/LogicalDevice/Queue。
  - 文件结构：`Swapchain/VulkanSwapchainCapabilities.cs`(查 caps/format/present mode/extent，纯数据) + `Swapchain/VulkanSwapchainOwner.cs`(建 Swapchain+Images+ImageViews，Dispose 先 ImageView 后 Swapchain) + `Swapchain/VulkanSwapchainLogFormatter.cs` + `Bridge/VulkanBridgeSwapchainAttachStep.cs`(在设备 step 后链式驱动)。
  - 红线继承：Bridge 已 100 行不再膨胀、DeviceOwner 不增职责、命名 VulkanDevice 别名/VulkanDeviceOwner/LogicalDevice、新增 .cs ≤100、UI 不触 Silk.NET.Vulkan、不复制 VulkanClearSession 旧探针。
- changelog.md 顶部新增 `[VK4-C-Plan]` 章节；file-tree.md 顶层摘要 + docs 列表补 `rz-vk4-c-swapchain-plan.md`、文件数 112→113。
- 提交 `5c0a75d`（docs/rz-vk4-c-swapchain-plan.md + changelog.md + file-tree.md），push 到 origin。本轮无代码改动、不构建。
- 下一步：规划通过后开 VK4-C（实装）→ VK4-C-R1（Resize 重建 Swapchain 审计）→ VK4-D（ClearFrame 出画面）。

## VK4-C Swapchain + Images + ImageViews 实装
- 用户拍板「开 VK4-C 实装，继续压边界」：只建 Swapchain + Swapchain Images + ImageViews；不建 RenderPass/Framebuffer/CommandPool/CommandBuffer、不 Clear/Present（仍黑屏为预期）；Resize 只重建 Swapchain+ImageViews（不重建 Surface/Instance/Device/Queue）；Dispose 顺序 ImageViews→Swapchain→LogicalDevice→Surface→Instance。
- 新增 `XuanYu.Render.Vulkan/Swapchain/` 4 文件：`VulkanSwapchainCapabilities.cs`(80) / `VulkanSwapchainBuilder.cs`(74) / `VulkanSwapchainOwner.cs`(77) / `VulkanSwapchainLogFormatter.cs`(13)；新增 `Bridge/VulkanBridgeSwapchainAttachStep.cs`(32) 在设备 step 后链式驱动。改写 `VulkanNativeHostSurfaceBridge.cs`(98，接近 100 红线，仅编排) + `VulkanBridgeLogFormatter.cs`(35，新增 `Emit` 统一出口)。全 .cs ≤100。
- 关键 API 坑（Silk.NET.Vulkan 2.22.0，经反射实测）：`KhrSurface` 方法无 `KHR` 后缀；枚举用非弃用短名 `ColorAttachmentBit`/`SpaceSrgbNonlinearKhr`/`MailboxKhr`/`FifoKhr`/`OpaqueBitKhr`/`ColorBit`；`TryGetDeviceExtension` 4 参。弃用 `*_Khr` 旧名改短名后两项目 0W0E。
- 红线校验：RenderPass/Framebuffer/CommandPool/CommandBuffer/Clear/Present grep 仅注释命中，无实装。Editor.UI 零改动仅随依赖编译。
- 文档同步：changelog.md 新增 `[VK4-C]` 章节（修正 VK4-C-Plan 口径「Bridge 已接近 100 行红线」非「已 100 行」）；file-tree.md 总文件 113→118、补 Swapchain 子目录与 VK4-C 状态；MEMORY.md 标 VK4-C 已实装、Bridge 98、加 Silk.NET 2.22.0 陷阱。
- 待 commit + push 到 origin `fix/RZ-VK3-A-surface-contract`；commit 后给 hash。

## VK4-C-Fix 运行前置修正（用户点名缺口，已实装待提交）
- 用户审计结论：VK4-C 代码边界合格，但 Swapchain 是第一个运行时易炸的 Vulkan 阶段，不能判「完全收口」；必须先做 VK4-C-R1 真机运行验证，严禁直接进 VK4-D。
- **最大风险点（用户点名，确为缺口已补）**：`VulkanDeviceOwner.Create` 原 `DeviceCreateInfo` 未启用 `VK_KHR_swapchain` 设备扩展 → 编译过、扩展函数拿得到，但 `CreateSwapchainKHR` 运行时会失败。修复：加 `EnabledExtensionCount=1` + `PpEnabledExtensionNames`（扩展名由 `VulkanSwapchainOwner.DeviceExtensionName` 传入，DeviceOwner 保持通用、不硬编码 swapchain 知识），96→99 行。
- **其余 R1 前置修正**：① `VulkanSwapchainOwner.Recreate` 加 0 尺寸跳过（`width<=0||height<=0` 记日志 return，不崩溃）；② `VulkanSwapchainOwner` 暴露 `Format`/`Extent`/`ImageViews` 只读供 VK4-D；③ `VulkanSwapchainBuilder.Build` 返回元组补 `Format`/`Extent`（74 行未增）；④ `VulkanBridgeDeviceAttachStep.Run` 传扩展名（29 行）；⑤ `VulkanNativeHostSurfaceBridge` 传 `VulkanSwapchainOwner.DeviceExtensionName`（98 行不变）。
- 红线校验：RenderPass/Framebuffer/CommandPool/CommandBuffer/Clear/Present grep 仅注释命中；Editor.UI 零改动；全 .cs ≤100（DeviceOwner 99 / SwapchainOwner 86 / Builder 74 / DeviceStep 29 / Bridge 98）；双项目低内存构建 0W0E。
- 新建 `docs/rz-vk4-c-r1-audit-plan.md`：VK4-C-R1 静态审计结论表（11 项全 ✅，能否完全收口 ⚠️ 还不能）+ 14 项运行验证清单 + 4 条严禁 + 可直接发 Codex 的指令。
- 状态标记：**VK4-C 代码完成 / VK4-C-R1 待运行验证 / VK4-D 暂缓**，勿写「完全收口」。
- 待提交（含本节点）：9 源码/文档 + MEMORY.md；push 后给 hash。

## VK4-C-R1 首次真机运行 + Resize 重建修复（ErrorNativeWindowInUseKhr）
- 用户真机运行 VK4-C-R1（RTX 3050 4GB Laptop GPU），贴回日志。
- ✅ 第一项：首次 Swapchain 创建成功（`Swapchain 创建成功；ImageView 创建成功 3 张`）—— 证明 VK4-C-Fix 启用的 `VK_KHR_swapchain` 设备扩展已生效（编译过≠能建，现确证能建）。
- ❌ 第二项：Resize 重建失败，两次 `CreateSwapchain 失败：ErrorNativeWindowInUseKhr`（713x549、713x188）；Resize 红线正确（日志 `不重建 Surface`）。
- ⚠️ 第三项：本次日志无关闭事件，Detach 顺序未验证。
- 根因：`VulkanSwapchainOwner.Recreate` 调 `Build` 建新 Swapchain 时旧 Swapchain 仍存在，且 `VulkanSwapchainBuilder.CreateSwapchain` 的 `SwapchainCreateInfoKHR` 未设 `OldSwapchain` → 驱动判窗口被旧 Swapchain 占用 → `VK_ERROR_NATIVE_WINDOW_IN_USE_KHR`。
- 修复：`Build` 加 `SwapchainKHR oldSwapchain = default` 透传；`CreateSwapchain` 设 `info.OldSwapchain = oldSwapchain`；`Recreate` 调 `Build(..., _log, _swapchain)` 传当前 Swapchain 作旧句柄；新建成功后再 `DestroyImagesAndViews` 退役旧 Swapchain（先 ImageView 后旧 Swapchain）。
- 构建：Render.Vulkan 0W0E；Editor.UI 先因 PID 8636 旧编辑器锁 bin 拷贝失败（MSB3027，非代码错），taskkill 释放后重建 0W0E；行数 Builder 74→75、Owner 86 不变，全 ≤100。
- 状态：VK4-C 待二次 R1 验证（Resize 成功 + Detach 顺序），未完全收口；VK4-D 暂缓。
- 待提交 + push，给 hash。

## 用户问「怎么测试」+ 补 SVG 可视化与人工测试清单 + 输出规定核对
- 用户问二次 R1 怎么测，并要求按输出规定补「SVG 可视化」与「人工测试清单」。
- 已渲染 `docs/vk4-c-r1-swapchain-fix.svg`（合规版：透明背景、字号≥11、规范箭头 marker），并在 changelog/file-tree 登记。
- 二次 R1 测试步骤（用户真机）：启动编辑器→看首次 Swapchain 创建成功→拖窗 Resize 看重建成功不再 `ErrorNativeWindowInUseKhr`→多次 Resize→0 尺寸跳过→关闭看 Detach 顺序 `ImageViews → Swapchain → LogicalDevice → Surface → Instance`。
- 人工测试清单 T1–T8：T1 首次创建已验✅；T2–T6 待二次 R1；T7 黑屏预期✅；T8 红线静态✅。
- 输出规定核对：工作流硬约定（push+hash+changelog/file-tree 进仓+VK4-D暂缓+不写凭据）全遵守（commit e81675a 已 push+给 hash）；「SVG可视化+测试清单」若为用户独立输出规定，前几轮交付（VK4-C-Fix/R1修复）漏了这两项，现补齐；若该规定有全文不在我能查的 rz-vk4-c-r1-audit-plan.md/MEMORY 内，请用户发来逐条核对。

