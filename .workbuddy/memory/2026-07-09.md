# 2026-07-09 工作日志

## LOG-UX-1-R2 日志面板自动滚动（2026-07-09 上午）
- 用户二次 R1 运行确认 VK4-C Resize 重建已通过（713x549 / 736x188 / 690x188 / 412x188 / 318x188 等均成功，无 `ErrorNativeWindowInUseKhr`）。但审计 Vulkan 生命周期时日志不自动滚到底，手动拖影响可用性。
- 用户指令称本轮为 LOG-UX-1-R1，但 changelog 中 LOG-UX-1-R1 已被 Ctrl+C 复制修复占用 → 顺延命名 **LOG-UX-1-R2**（避免两同名章节）。
- 实现（`XuanYu.Editor.UI/Foot/Foot.axaml.cs` 36→89 行）：
  - `TryHook` 经 `AttachedToVisualTree`+`DataContextChanged` 双重挂接（防重复订阅）；`FindDescendantOfType<ScrollViewer>()` 取内部 SV。
  - 跟随判定 `LogScroll_OnScrollChanged`：**仅当 `|OffsetDelta.Y|>=0.5`（用户主动滚动）才重算 `_followTail`**，`Offset.Y+Viewport.Height >= Extent.Height-2` 视为在底部；Extent 增长（新日志）不误判 → 规避「用户在底部却被新日志误停跟随」竞态。
  - 实际滚动放 `LogList_OnLayoutUpdated`：`_pendingScroll` 一次性标志，布局完成后再 `ScrollToEnd()`，确保新项已测量。
  - `UiVm.PropertyChanged` 仅在 `LogItems` 变化置 `_pendingScroll=_followTail`；首次附着若 `_followTail` 即对齐底部。
- 红线守住：`UiVm.Logging.cs` 保持 100 行未动（不塞逻辑）、`Foot.axaml`/`EditorLogBuffer`/`EditorLogBus` 未动、不碰 Vulkan/Render.Vulkan/NativeHost。
- Editor.UI 低内存构建 **0W0E**（PID 9468 旧编辑器锁 bin，taskkill 释放后重建通过）。
- changelog.md 新增 LOG-UX-1-R2 章节；file-tree.md Foot.axaml.cs 行数 36→89。

## VK4-C 状态更正（重要）
- Detach 释放日志**已存在于代码**，无需另开 Vulkan 改动轮：
  - `VulkanSwapchainOwner.Dispose` → `VulkanSwapchainLogFormatter.Disposed()` = `【VulkanSwapchain】Swapchain 释放成功`（内部 `DestroyImagesAndViews` 先 ImageView 后 Swapchain，与单次日志合并）。
  - `VulkanDeviceOwner.cs:95` = `【VulkanDevice】LogicalDevice 释放成功`。
  - `VulkanBridgeLogFormatter` = `Surface 已释放` / `Instance 已销毁` / `分离完成：Surface + Instance 已释放`。
  - 顺序即 ImageViews→Swapchain→LogicalDevice→Surface→Instance，正确。
- 用户原话「再补 VK4-C 的关闭释放日志」是误判；补自动滚动后重跑即可见 T6 证据。
- VK4-C 当前：T1/T2/T3/T4 ✅（二次 R1）、T7 黑屏 ✅、T8 静态红线 ✅、T5 0尺寸未触发、T6 Detach 顺序待重跑确认（自动滚动补丁后首关即可看）。

## 待办
- 用户重跑编辑器：验证自动滚动跟随 + 关闭后看 T6 Detach 顺序日志；两项过则 VK4-C 正式收口、开 VK4-D。
- 提交 LOG-UX-1-R2（Foot.axaml.cs + changelog + file-tree + 本日志）push origin 后给 hash。

## LOG-UX-1-R3 自动滚动修复 + WinExe 控制台输出（2026-07-09 上午）
- **R2 自动滚动不生效**：用户反馈真机运行日志面板根本不自动滚动。
  - **根因**：R2 用 `LayoutUpdated` 事件触发 `ScrollToEnd`，但 Avalonia 的 LayoutUpdated 触发时机与 PropertyChanged(LogItems) 时序不可靠——新日志写入 buffer → RefreshLogBindings → 设置 _pendingScroll=true，但 LayoutUpdated 可能在设置前已触发过、或 ListBox 虚拟化延迟导致不再为本次变更触发。
  - **修复**：改用 `Dispatcher.InvokeAsync(ScrollToTail, DispatcherPriority.Render)` —— 显式放入 dispatcher 队列的 Render 优先级，确保布局完成后再滚，比事件驱动可靠。去掉了 R2 自定义 DispatcherTimerExt 辅助类。Foot.axaml.cs 89→91 行。
- **WinExe 控制台无输出**：用户贴截图显示 `dotnet run` 终端只有 build 输出，Vulkan Console.WriteLine 全部不可见。
  - **根因**：`<OutputType>WinExe</OutputType>` — Windows 上 WinExe 进程不继承父控制台句柄，Console.WriteLine 写入虚空。
  - **影响**：已有 6 处 Console.WriteLine（BridgeLogFormatter.Emit / DeviceOwner.Log / SwapchainOwner.Log / Capabilities.Log / Builder.Log / Selector.Log）全部无效。
  - **修复**：`Program.cs:Main()` 首行调用 `[DllImport("kernel32")] AttachConsole(-1)`（ATTACH_PARENT_PROCESS），零改动 Vulkan 代码。
  - **效果**：关闭编辑器后终端仍显示完整 Detach 释放序列 → 直接解决 T6 审计问题。
- Editor.UI 低内存构建 **0W0E** ✅（Foot.axaml.cs 91 行 / Program.cs 28 行，均 ≤100）。

## LOG-UX-1-R4 日志系统三修（2026-07-09 上午）
- **自动滚动根因确证**：R2/R3 失效因为 `TryHook` 时 ListBox 模板未应用，`FindDescendantOfType<ScrollViewer>()` 返回 null 且不再重试 → `_logScroll` 永远 null → 滚动死。修复：`LogList.TemplateApplied` + `Dispatcher.InvokeAsync(Resolve, Loaded)` 延迟重试拿到 ScrollViewer；新日志 `Dispatcher.InvokeAsync(ScrollToEnd, Render)`。Foot.axaml.cs 91→98 行。
- **种子日志清理**：`UiVm.Logging.InitLogs` 删 `_logBuffer.Seed(SampleLogEntries.All)` + 3 条过期种子 `_logBus.Info`（含「Device/Swapchain 尚未接入」）。空状态用 UI「暂无日志」占位。
- **控制台去重**：低层 `Log(log,m)` 辅助（DeviceOwner/SwapchainOwner/Capabilities/Builder/Selector）+ Selector 2 内联 + 3 个 Bridge AttachStep 共 5 处均有 `log?.Invoke(m); Console.WriteLine(m)`；`log`=Emit→`VulkanBridgeLogFormatter.Emit` 已写控制台 → 双输出。统一删低层 Console.WriteLine，仅留 `VulkanBridgeLogFormatter.Emit` 单出口。`VulkanInstanceOwner`/`VulkanSurfaceOwner` 仅直接 Console.WriteLine（不走 Emit），终端已单现，未动。
- **事故**：Edit 误将 C# 写入 `Foot.axaml`（XAML）文件，立即用原始 XAML 覆盖还原、C# 改写 `.cs`，已校验 XAML 首行正常、双项目 0W0E。
- 双项目低内存构建 **0W0E**；全 .cs ≤100；不碰 Vulkan 生命周期/NativeHost/Swapchain 逻辑。

## LOG-UX-1-R5 崩溃修复（2026-07-09 上午）
- **用户 run.bat 贴日志+截图**：Vulkan 链路全过（Instance/Surface/9候选/RTX3050/Device/Queue/Swapchain+3IV ✅），但编辑器窗口显示「未响应」、退出码 `-805306369`（0xCFFFFFFF）崩溃。
- **根因（确认）**：R4 的 `OnVmPropertyChanged` 改动——每条 Vulkan 日志都调 `ResolveScrollViewer()` → `FindDescendantOfType<ScrollViewer>()` 遍历视觉树。而 `_bridge.Attach()` 在 `OnAttachedToVisualTree` 内**UI 线程同步执行** 25 条日志 × 每条遍历视觉树 + null 时堆积 `Dispatcher.InvokeAsync(Loaded)` 重试 → UI 线程阻塞 → Windows 判定未响应 → CLR 异常终止。
- **修复**：恢复 R3 null-guard 早返回语义——`if (!_followTail || _logScroll is null) return;`，_logScroll 为 null 时不做任何事。`ResolveScrollViewer` 只从 `TemplateApplied`/`AttachedToVisualTree`/`DataContextChanged` 三处模板生命周期事件驱动。保留 TemplateApplied 延迟解析（R4 真正改进）。
- Foot.axaml.cs **96 行 ≤100** ✅；双项目 **0W0E** ✅。
- Commit: `35e74f9`，已推送 origin。待用户 run.bat 验证三项：①不崩溃 ②自动滚动生效 ③无种子/控制台不重复。

## LOG-UX-1-R5A 止血：彻底禁用自动滚动（2026-07-09 上午，接 R5）
- 用户判断与指令：未响应是 **UI 线程卡死**，非 Vulkan 失败。截图「玄域编辑器（未响应）」+ 终端已到 ImageView 创建成功 3 张 → Vulkan 链路全过。黑色大块是 Windows DWM 未响应残影，非代码 bug。
- 用户明确：不要再在 `Foot.axaml.cs` 叠自动滚动补丁。应做 **LOG-UX-1-R5A 止血**——禁用自动滚动，保留其余 LOG-UX 成果。
- **保留**：控制台日志去重、种子清理、Ctrl+A/Ctrl+C 多行复制、详情换行、AttachConsole。
- **禁用/移除**：`Foot.axaml.cs` 全部自动滚动逻辑（ResolveScrollViewer / HookVm / OnVmPropertyChanged / OnScrollChanged / ScrollToTail / TemplateApplied 订阅）。
- `Foot.axaml.cs` 由 96 行精简至 **42 行**；双项目 **0W0E** ✅。
- Commit: `8407657`，已推送 origin。
- 后续：自动滚动由 **LOG-UX-2** 重新设计——拆出 `Foot/LogListAutoScrollController.cs`，`Foot.axaml.cs` 只负责创建 controller + 把 ListBox 交给它 + 收到新日志通知它；controller 内部做节流（已安排滚动则不重复安排，等布局完成只滚一次）。
- 当前阶段：VK4-C Vulkan 链路通过；VK4-D 暂停；先稳定编辑器再重开自动滚动设计。

## LOG-UX-2 自动滚动重设计：独立控制器（2026-07-09 下午）
- 按用户给定方案执行：拆 `Foot/LogListAutoScrollController.cs`（74 行），`Foot.axaml.cs` 只做接线（创建 controller + 选中 + Ctrl+A/Ctrl+C），64 行。
- **防卡死四机制**：①单次解析（`_resolved` 守卫，仅 TemplateApplied 后 `FindDescendantOfType<ScrollViewer>` 一次，不每条日志遍历）②节流（`_pendingScroll` 连续多条只排一次 `Dispatcher.UIThread.InvokeAsync(ScrollToTail, Render)`）③防重入（`_isProgrammaticScroll` 期间 ScrollChanged 直接 return）④`OnVmPropertyChanged` 只做布尔判定，Vulkan Attach 期 `_scroll` 为 null → 直接返回零遍历。
- `TemplateAppliedEventArgs` 命名空间在 Avalonia 12 为 `Avalonia.Controls.Primitives`（缺 using 会编译失败）。
- 构建：残留 `XuanYu.Editor.UI (32896)` 进程锁 DLL 报 MSB3027 → `Stop-Process -Id 32896 -Force` 杀掉后重建 **0W0E** ✅。
- 待用户 run.bat 真机验收：稳定启动+自动滚到底+上翻不禁+回底恢复+复制/详情/Vulkan 单次日志保持。

## 收口确认 + VK4-D-Plan 启动（2026-07-09 晚，用户真机验证反馈）
- **用户确认 LOG-UX-2 真机通过** → Foot 面板不再「未响应」，自动滚动稳定；VK4-C 链路（Instance→Surface→Device→Queue→Swapchain→ImageView→Resize 重建→Detach 逆序释放）确认正式收口。
- **三问题最终归因（用户总结，作防回潮基线）**：①UI 未响应=自动滚动堆 Foot.axaml.cs 视觉树查找+Dispatcher+ScrollChanged 套娃 → 拆 LogListAutoScrollController；②控制台日志重复=多处低层 Console.WriteLine 与统一 Emit 双出口 → 保留单出口；③21:32 假日志=示例/种子混入真实日志 → 清理。
- **VK4-C / LOG-UX-2 正式收口记录入 changelog + MEMORY**；LOG-UX-2 收口 Commit `a7149f6`。
- **开 VK4-D-Plan**：新建 `docs/rz-vk4-d-plan.md`，目标最小 Clear+Present 单色清屏闭环。首次涉及 RenderPass/Framebuffer/CommandPool/CommandBuffer/Semaphore/Fence/AcquireNextImage/QueueSubmit/QueuePresent。红线：只做最小清屏，不做场景渲染/相机/网格/材质/Gizmo/UI叠加/持续动画。边界：Resize 只重建 Framebuffers；Detach 顺序 ClearFrame→Swapchain→Device→Surface→Instance；Present 泵独立线程禁 UI 线程。为守 Bridge ≤100 红线，实装时顺带引入薄组合根 `VulkanRenderSession`（原 VK4-E 范围），Bridge 委托。
- 阶段判断：VK4-A/B/C 完成；LOG-UX-1/2 收口；**进入 VK4-D-Plan**，不回头补日志功能。
- 下一步：用户确认规划后开 VK4-D 实装（计划态不改代码，仅文档 + 记录）。
- **VK4-D 实装（D1+D2+D3 同轮）完成**：按 `docs/rz-vk4-d-plan.md` 落地最小单色清屏闭环。新增 `Render/VulkanClearFrameOwner.cs`(93) + `Render/VulkanPresentLoop.cs`(86) + `Render/VulkanClearFrameLogFormatter.cs`(13) + `Session/VulkanRenderSession.cs`(59) + `Bridge/VulkanBridgeRenderSessionAttachStep.cs`(15)；`VulkanSwapchainOwner.cs` 补 `Swapchain`/`Khr` getter(88)；`VulkanNativeHostSurfaceBridge.cs` 压回 84 行（Attach 经 step 建 `_renderSession`、Resize 转发、Detach 首行 Dispose）。
- **技术校验**：用临时反射探针（`/d/tmp_vkprobe`，已弃）确认 Silk.NET.Vulkan 2.22.0 真实 API——`KhrSwapchain` 方法名**无 KHR 后缀**（`AcquireNextImage`/`QueuePresent`），`SampleCountFlags.Count1Bit`，`ImageLayout.PresentSrcKhr`，`StructureType.PresentInfoKhr`，`ClearColorValue.Float32_0..3`；`Semaphore` 别名消 `System.Threading` 歧义；内联 `new XxxCreateInfo{}` 触发 CS9193（ref readonly），须改局部变量。
- **运行时正确性**：Present 泵独立 `Thread(IsBackground)`；单 in-flight 帧 + 单 Fence + `_submitted` 守卫（避免首帧 WaitForFences 空等 1s）；Acquire 指针重载 + CommandBuffer[] 才 `fixed`；OUT_OF_DATE/SUBOPTIMAL 当帧 continue 交 Resize 重建；Detach 先 Stop 泵再释放（顺序 ClearFrame→Swapchain→Device→Surface→Instance）。
- **构建**：双项目低内存构建 **0W0E**；全 .cs ≤100。待用户 run.bat 真机验收「单色清屏画面 + Resize 恢复 + 关闭释放顺序」。
- **VK4-D-R1 修复完成**：
  - Resize 去重：`VulkanNativeHostSurfaceBridge.Resize` 删除直接 `_swapchainOwner?.Recreate`，仅转发 `RenderSession.Resize`；统一入口 `Stop → Recreate → Rebuild Framebuffers → Start`，日志只出现一次 Swapchain 重建。
  - 首帧 Present 可观测：新增 `VulkanClearFrameLogFormatter.FirstPresented(idx)`，PresentLoop 首次 QueuePresent 成功后打印一次。
  - Present 错误可见：`AcquireNextImage` / `QueuePresent` 非 Success/SuboptimalKhr 时输出中文错误。
  - 修正 `SuboptimalKhr` 处理：初版把该成功码当失败 break 导致泵首帧退出、画面黑；R1 改为允许 SuboptimalKhr，ErrorOutOfDateKhr sleep continue 等 Resize。
  - clear 颜色改为 0.25/0.45/0.70 明显蓝灰。
- **行数**：Bridge 83 / ClearFrameOwner 93 / PresentLoop 92 / LogFormatter 14 / RenderSession 59 / Step 15，均 ≤100。
- **构建**：双项目低内存构建 **0W0E**。待用户 run.bat 真机验收：单色清屏画面、首帧 Present 日志、Resize 单次重建、关闭释放顺序。

## VK4-D-R2 后台线程日志回调线程派发修复（2026-07-09 晚，接 VK4-D-R1）
- **用户 run.bat 真机闪退**：启动即崩，退出码 `-532462766`；Vulkan 链路已全过（RenderPass+Framebuffer 创建成功、Present 泵已启动），崩在后台线程日志。
- **异常**：`System.InvalidOperationException: The calling thread cannot access this object because a different thread owns it`；堆栈 `VulkanNativeHost.<OnAttachedToVisualTree>b__6_0` 访问 `DataContext` → 来自 `VulkanPresentLoop.Run` 独立后台线程。
- **根因**：VK4-D 引入独立 Present 线程后，`VulkanPresentLoop.Log` → `VulkanBridgeLogFormatter.Emit` → `VulkanNativeHostSurfaceBridge.Emit` → 回调 `msg => ViewportNativeHostRoute.ReportVulkanBridge(DataContext as UiVm, msg)` 在非 UI 线程直接访问 Avalonia `DataContext`（仅 UI 线程可访问）→ 未处理异常闪退。
- **修复（只修线程派发，不碰渲染/Resize/Bridge 委托）**：
  - `XuanYu.Editor.UI/Viewport/Vulkan/VulkanNativeHost.cs`：内联回调改为 `ReportVulkanMessage`；`Dispatcher.UIThread.CheckAccess()` 判断，UI 线程直调 `ReportVulkanMessageOnUiThread`，否则 `Dispatcher.UIThread.Post(() => ...)` 回 UI 线程再访问 `DataContext`/`UiVm`/日志集合；新增 `using Avalonia.Threading;`。文件 83→**95 行**。
  - `XuanYu.Render.Vulkan/Render/VulkanPresentLoop.cs`：`Log` 由 `_log?.Invoke(m)` 改为 `try { _log?.Invoke(m); } catch { }`，后台线程日志异常被吞、不终止泵/不炸进程（第二层保护，Render.Vulkan 仍不引用 Avalonia）。92→**96 行**。
- **分层守纪**：`Render.Vulkan` 仅持有 `Action<string> log` 契约，线程安全由消费方保证；`VulkanBridgeLogFormatter.Emit` 单出口不变（先 `log?.Invoke` 后 `Console.WriteLine`）。
- **构建**：双项目低内存构建 **0W0E**；全 .cs ≤100（VulkanNativeHost 95 / VulkanPresentLoop 96 / Bridge 83 / ClearFrameOwner 93 / RenderSession 59 / Step 15 / LogFormatter 14）。
- 待用户 run.bat 真机验收：启动不闪退、控制台「Present 泵已启动」+「首帧 Present 成功」、视口蓝灰、Resize 单次重建、关闭释放顺序正确。全过 → VK4-D 正式收口。

## VK4-D-R3（Render.Vulkan 侧，2026-07-09 晚，接 VK4-D-R2）
- **用户两阶段方案**：VK4-D-R2 收口后真机发现 ①蓝灰只覆盖上半部分（半屏）②QueuePresent 反复 ErrorOutOfDateKhr 刷屏 ③日志详情栏展开/收起后 NativeHost 视口同步慢半拍 ④Resize 时序竞争。分 VK4-D-R3（Render.Vulkan 侧，修 ①②）与 VIEWPORT-RESIZE-R1（Editor.UI 侧，修 ③）两轮，各自独立 commit push。
- **核对半屏成因**：读 6 个 Render.Vulkan 源后确认 extent 已统一——`VulkanSwapchainOwner._extent`（=caps.CurrentExtent 物理像素）、Framebuffer/RenderPass/RenderArea 同源。用户方案「Framebuffer 用逻辑尺寸」假设不成立 → 不盲改正确的 extent 统一，只做诚实日志让半屏真因在真机暴露。
- **VK4-D-R3 改动（6 文件，全 ≤100）**：
  - `Render/VulkanPresentLoop.cs` 96→**99**：`QueuePresent` 返回 `ErrorOutOfDateKhr` 时 `_outOfDateLogged` 守卫仅记一次 `OutOfDatePaused()` 后 break，不刷屏；`Start()` 重置该标志；`Stop()` 局部捕获 `var t=_thread` 防 NRE。
  - `Session/VulkanRenderSession.cs` 59：Resize 顺序收口 `Stop→Recreate→RebuildFramebuffers→记 Rebuilt(_swapchainOwner.Extent 物理像素)→Start`，Rebuilt 在 Start 前。
  - `Swapchain/VulkanSwapchainCapabilities.cs` 80→81：能力日志打印 请求逻辑尺寸 + Surface CurrentExtent（物理像素）+ 选择 extent（物理像素）。
  - `Swapchain/VulkanSwapchainLogFormatter.cs` 13→15 / `Render/VulkanClearFrameLogFormatter.cs` 14→17：改收 `Extent2D` 打印「（物理像素）」；后者新增 `OutOfDatePaused()`。
  - `Swapchain/VulkanSwapchainOwner.cs` 88：`Created`/`Recreated` 改传实际 `_extent`。
- **构建**：杀残留 `XuanYu.Editor.UI (5892)` 锁 DLL 进程 → 低内存构建 **0W0E ✅**（Render.Vulkan + Editor.UI 均生成 dll）。
- **VIEWPORT-RESIZE-R1 尚未开始**：先加中文 probe 确认时序，再布局稳定后主动同步最终尺寸；拖动仍走 Coalescer；Editor.UI 不碰 Silk.NET.Vulkan。
- **已提交并推送**：commit `56a0d79`（8 文件 +66/-11），push origin `fix/RZ-VK3-A-surface-contract`。changelog.md/file-tree.md 已更新。
- **待用户 run.bat 真机验收 VK4-D-R3**：①全区域蓝灰不再半屏（看物理像素 extent 日志）②Resize 单次重建且日志顺序 Rebuilt 在 Start 前 ③OutOfDate 不再刷屏仅记一次。全过则开 VIEWPORT-RESIZE-R1（Editor.UI 侧修 ③视口同步慢半拍）。

## VIEWPORT-RESIZE-R1（Editor.UI 侧，2026-07-09 晚，接 VK4-D-R3）
- **根因（用户二次 run.bat 日志 + 截图确认）**：半屏蓝灰不是 Render.Vulkan extent 错，而是 NativeHost 子窗口尺寸同步慢半拍。日志详情栏展开/收起是低频离散布局变化，但 Vulkan Swapchain 重建走 250ms Coalescer —— 期间 HWND 已 `OnSizeChanged` 立即 resize、Surface 已 OutOfDate、Present 泵已停，旧小帧冻结在顶部、下方黑。用户方案「NativeHost 子 HWND 没填满 Viewport」与主嫌疑吻合。
- **修复（修法 A/C，全 ≤100，不动 Render.Vulkan）**：
  - `VulkanNativeHost.cs` 94→**99**：`partial` 化；构造函数订阅 `DataContextChanged` 钩 `HookLayoutSync`；`OnDetachedFromVisualTree`/`DestroyNativeControlCore` 加 `UnhookLayoutSync`；不动原有 `OnSizeChanged`/Coalescer 路径。
  - 新增 `VulkanNativeHost.LayoutSync.cs`（**38**）：`HookLayoutSync`/`UnhookLayoutSync` 订阅 `UiVm.IsLogOpen` 的 `PropertyChanged`；`OnLayoutSyncProp` 以 `Dispatcher.UIThread.InvokeAsync(SyncFinalSize, DispatcherPriority.Render)` 调度；`SyncFinalSize` 读最终 `Bounds` → `_resizer.Cancel()`（撤销待处理 250ms Coalescer）→ `Win32ViewportHost.GetClientSize` 探针 → `Win32ViewportHost.Resize` + `_bridge.Resize` 立即同步最终尺寸，消除慢半拍。
  - `Win32ViewportHost.cs` 55→**67**：新增 `GetClientSize(hwnd)`（P/Invoke `GetClientRect` + `RECT`），取 Win32 子窗口物理像素供探针核对。
  - `ViewportNativeHostRoute.cs` 15→18：新增 `ReportProbe` → `UiVm.LogNativeHostProbe`。
  - `UiVm.NativeHostLifecycle.cs` 27→40：新增 `LogNativeHostProbe`（日志栏状态/逻辑尺寸/子窗口物理尺寸/DPI 四者对齐探针）。
- **红线守住**：不重建 Surface/Instance/Device/Queue；不改 RenderPass/CommandBuffer/PresentLoop 主逻辑；不碰 LOG-UX 自动滚动；Editor.UI 不接触 Silk.NET.Vulkan；全 .cs ≤100。
- **构建坑**：用户 run.bat 起的 `XuanYu.Editor.UI (8800)` 锁 bin DLL → 默认输出构建 MSB3027 失败；改临时 `-p:OutputPath=.verify_build` 又撞沙箱 OOM（`0x8007000E` 加载 CoreLib 失败）。最终杀 8800 后默认输出低内存构建 **0W0E ✅**（4 项目均生成 dll）。
- **已提交并推送**：commit `b9b2a63`（7 文件 +115/-3），push origin `fix/RZ-VK3-A-surface-contract`（父 `56a0d79`）。changelog.md/file-tree.md 已更新 VIEWPORT-RESIZE-R1 快照。
- **待用户 run.bat 真机验收 VK4-D 收口**：①启动不闪退不未响应 ②蓝灰覆盖整个 NativeHost 区域不再半屏黑 ③打开/关闭日志详情栏视口立即同步、不慢半拍 ④日志显示 NativeHost 逻辑尺寸×DPI 与 Win32 子窗口物理尺寸对齐 ⑤Resize 不重建 Surface/Instance/Device/Queue ⑥关闭释放顺序正确。全过 → VK4-D 正式收口。

## VIEWPORT-RESIZE-R2（Editor.UI 侧，2026-07-09 晚，接 R1）
- **根因（用户三次 run.bat 日志 + 截图判死）**：R1(b9b2a63) 把 Avalonia `Bounds` 的**逻辑尺寸**直接当物理像素喂 `Win32ViewportHost.Resize`（裸 SetWindowPos 不乘 DPI），把子 HWND 缩成 713×188；探针日志 `Win32子窗口=713x188`、`Surface CurrentExtent=713x188`，蓝灰只占左上角。真因：`Bounds` 是逻辑像素，Win32/Vulkan 要物理像素，R1 少了 ×DPI 换算、绕过了 Avalonia 本来的 DPI 感知摆放（之前 1248×961 是 Avalonia NativeControlHost 按物理摆放盖过了 OnSizeChanged 的逻辑 resize，R1 在 Render 优先级手动改回逻辑尺寸把正确摆放覆盖了）。
- **修复（全 ≤100，不动 Render.Vulkan）**：
  - `VulkanNativeHost.LayoutSync.cs` 38→**45**：`SyncFinalSize` 先 `physical=max(1,round(logical×GetDpiScale()))`，`Win32ViewportHost.Resize(_hwnd, physicalW, physicalH)` 收物理像素；`_bridge.Resize(logicalW, logicalH)` 仍收逻辑（Render.Vulkan 侧用 caps.CurrentExtent 建 Swapchain，与 VK4-D-R3 同源）；`GetClientSize` 取实际物理供探针。`_resizer.Cancel()` 保留，拖动仍走 OnSizeChanged→Coalescer。
  - `ViewportNativeHostRoute.cs` 18：`ReportProbe(vm, open, logicalW, logicalH, dpi, targetW, targetH, clientW, clientH)`。
  - `UiVm.NativeHostLifecycle.cs` 40：`LogNativeHostProbe` 输出 目标物理 + 子窗口实际；验收须看到 `逻辑×DPI ≈ 目标物理 ≈ Win32子窗口 ≈ Surface CurrentExtent`（DPI=1.75 时 713×188→1248×330）。
- **红线守住**：不重建 Surface/Instance/Device/Queue；不改 RenderPass/PresentLoop；不碰 LOG-UX；Editor.UI 不触 Silk.NET.Vulkan；全 .cs ≤100。
- **构建**：低内存构建进行中（任务 OBZshF）。
- **待构建 0W0E 后**：提交并 push origin `fix/RZ-VK3-A-surface-contract`，给 hash；请用户 run.bat 验收 R2 6 项（全过 → VK4-D 正式收口）。

## VIEWPORT-RESIZE-R2 收口完成（2026-07-09 晚，本会话收尾）
- **构建验证**：OBZshF 任务随会话重置丢失，重新执行低内存构建（task `0wSZbb`）→ 双项目 0W0E ✅：`XuanYu.Core`/`XuanYu.Render.Abstractions`/`XuanYu.Render.Vulkan`/`XuanYu.Editor.UI` 均生成 dll，0 警告 0 错误。Render.Vulkan 连带编译通过=未触碰证据。
- **.cs ≤100 全检**：Python 扫 5 项目源码（排除 bin/obj），0 个 >100 文件。改动三文件实测行数 = VulkanNativeHost.LayoutSync.cs **49** / ViewportNativeHostRoute.cs 18 / UiVm.NativeHostLifecycle.cs **38**（VulkanNativeHost.cs 98 / Win32ViewportHost.cs 67），均 ≤100。
- **改动范围核对**：`git status` 仅 Editor.UI 3 个 .cs + changelog.md + file-tree.md 改动，Render.Vulkan / Abstractions / Core / Editor.Win 零改动 → 满足「只改 Editor.UI NativeHost/LayoutSync 路径 + 文档、不动 Render.Vulkan、不重建 Surface/Instance/Device/Queue、不改 PresentLoop/RenderPass/Framebuffer 主逻辑、不改 LOG-UX」。
- **文档校正**：changelog.md 顶部补 `[VK4-D] 正式收口确认` 章节（运行态 6 项验收 + 红线 + 下一阶段 VK5 规划）；R2 条目状态由「待验收」改「已收口（用户真机 2026-07-09 通过）」；R2/红线两处行数 99→98、45→49、40→38 按实测更正。file-tree.md 同三处行数更正（98/49/38）。
- **临时 commit message 文件被系统清理**：`git commit -F <Temp 路径>` 两次报 No such file；改用 `git commit -F - <<'EOF'` heredoc 直接读 stdin 提交成功。
- **已提交并推送**：commit **`692a7b244a99879774348998dc99df195dd062fe`**（短 692a7b2），5 files changed +74/-24，push origin `fix/RZ-VK3-A-surface-contract`（b9b2a63..692a7b2）。
- **收口判定（用户 2026-07-09 真机确认）**：VIEWPORT-RESIZE-R2 运行验证通过；VK4-D 关闭释放顺序沿用 VK4-D-R3 已验证序列（Present 泵停止→ClearFrame→Swapchain→LogicalDevice→Surface→Instance→分离完成），R2 未触碰 Release 路径 → **VK4-D 正式收口**。
- **下一步**：VK4-D 收口文档归档 + VK5 规划（场景渲染/相机/网格/材质/Gizmo/UI 叠加，均不在 VK4-D 红线内）。建议用户再跑一次关闭日志做最终确认（收口后可选）。

## VK4-Closure + VK5-Plan 文档轮（2026-07-09 晚，本会话收尾，仅文档）
- 按用户指令进入「VK4 收口 + VK5 规划」阶段，**只写文档，不写实装代码**（未改任何 .cs/.axaml/.csproj/Vulkan/LOG-UX/NativeHost）。
- 新增 `docs/rz-vk4-closure.md`：VK4-A/B/C/D + VIEWPORT-RESIZE-R2 逐项收口表、已验证清单（首帧 Present/蓝灰覆盖/Resize 恢复/详情栏不慢半拍/日志单出口/双项目 0W0E/全 .cs ≤100）、跨阶段长期硬规则（Bounds 逻辑 / Win32+Surface 物理 / physical=round(logical×DPI) / Render.Vulkan 不引用 Avalonia / UI 不接触 Silk.NET.Vulkan）、已知债务（Editor.UI 仍引用 Render.Vulkan、VulkanClearSession 死代码待清）。
- 新增 `docs/rz-vk5-plan.md`：VK5 最小几何渲染规划——VK5-A Shader+Pipeline / VK5-B gl_VertexIndex 画固定三角形（暂不建 VertexBuffer）/ VK5-C viewport/scissor 动态状态同步 extent（Resize 不重建 Pipeline）/ VK5-D Clear/Draw 职责收进清晰 RenderPass/FrameRenderer 边界（不进场景系统）。12 条红线、资源创建/释放顺序、文件结构建议（Pipeline/ 子目录）、SVG 规划图。
- changelog.md 顶部加 `[VK4-Closure + VK5-Plan]` 章节；file-tree.md 加同名文档快照。
- 关键决策：VK5 第一步**不是场景渲染**，而是「固定三角形 / 最小 Pipeline / 最小 Draw」；规划通过后再开 VK5-A 实装。

## VK5-Plan 债务升格 + fe6d5d3 提交审计（2026-07-09 晚，接文档轮）
- 用户审计结论：文档轮可接受，但须补查 `fe6d5d3` 提交文件清单、确认无 AI memory/临时文件误提交。
- **审计结果（已 `git show --name-only` 复核）**：`fe6d5d3` 仅含 6 文件——`.workbuddy/memory/2026-07-09.md` + `.workbuddy/memory/MEMORY.md`（**项目级工作记忆**，Layer 3 工作区记忆，非用户级 AI 文件 `C:\Users\35013\.workbuddy`）+ `changelog.md` + `docs/rz-vk4-closure.md` + `docs/rz-vk5-plan.md` + `file-tree.md`。`.gitignore` 未含 `.workbuddy`，但其已惯例追踪（`git ls-files` 显示 07-07/08/09 + MEMORY.md 早已在库），无 `.codex/`/`.ai-memory/`/token/密钥 → **无泄漏，问号消**。
- **债务升格（用户明确要求，纯文档）**：
  - `docs/rz-vk5-plan.md` 新增 §12「架构债务升级」：债务 A（`Editor.UI` 仍引用 Render.Vulkan）升格为**架构债务**，锁三原则（VK5-A~D 可暂缓但**禁止扩大**；VK6/独立 ARCH 轮必须收口到 `Render.Abstractions`）；债务 B（`VulkanClearSession` 死代码）清理排 **VK5-E**（VK5-A/B/C/D 之后），不在 VK5-A 前清。原 §12 规划图顺延 §13。
  - `MEMORY.md` 新增「架构债务（升格，锁死节奏）」节；`changelog.md` 顶部加 `[VK5-Plan 债务升格]` 章节。
- **红线守住**：本轮仅改 3 份文档（rz-vk5-plan.md / changelog.md / MEMORY.md）+ 工作日志，**未改任何 .cs/.axaml/.csproj/Vulkan/LOG-UX/NativeHost 代码**。
- **下一步（用户拍板）**：确认 fe6d5d3 无泄漏后，再开 **RZ-VK5-A**（ShaderModule + GraphicsPipeline 最小接入，不 Draw、不画三角形、不建 VertexBuffer、不接场景/相机/材质/Gizmo、不改 UI/NativeHost/LOG-UX/Resize）。VK5-A 实装前须先读 rz-vk4-closure/rz-vk5-plan/当前 Vulkan Clear+Present 文件/file-tree/dev-rules，输出文件清单+职责边界+创建/释放顺序+≤100 拆分+不做清单，**确认前不写代码**。

